# UI / UX 점검 기록 (사용자 관점)

> 대상: toeic.monster 정적 사이트 전체 (`index.html` 앱 + `units/` · `guides/` · `grammar/` 정적 페이지)
> 점검일: 2026-09-14 · 방법: 실제 마크업/CSS·데이터 계측 + 메인 페이지 학습 흐름(진입 → 탐색 → 학습 → 복습) 시뮬레이션
> 2차 점검: 2026-09-15 · 방법: 1차와 같은 계측 + 첫 로드 비용(전송량·DOM 노드 수)·목차 묶음 구성 측정 → 결과와 조치는 **§7**
> 3차 점검: 2026-09-15 · 방법: 헤드리스 Chrome 으로 실제 첫 화면·모바일 폭·OS 다크 모드 계측 + §7-9 에 남긴 5건 처리 → 결과와 조치는 **§8**
> 4차 점검: 2026-09-15 · 방법: 전송량 계측(공유 카드·정적 페이지 공용 자산) + 정적 페이지 10개를 실제 브라우저로 계측 → 결과와 조치는 **§9**
> 5차 점검: 2026-09-15 · 방법: 프리렌더 확장 · 앱 화면 11개 히트 영역 · 배포 실주소 점검 → 결과와 조치는 **§10**
> 6차 점검: 2026-09-15 · 방법: 사용자 제보 3건(모바일 햄버거 메뉴 스크롤 · 뒤로가기 · 낭독 이모지) 재현 + 헤드리스 Chrome 계측 → 결과와 조치는 **§11**
> 7차 점검: 2026-09-15 · 방법: 사용자 제보(PC 창을 줄이면 상단바 메뉴가 잘려 스크롤되지 않음) 재현 + 데스크톱 폭·창 높이 9종(768×560~1920×1080) 계측 → 결과와 조치는 **§12**
> 8차 점검: 2026-09-16 · 방법: 홈 스크롤 높이 계측(375·1100px · `tools/measure-home.mjs`) + 목차 4묶음과 본문 순서 대조 + 접힌 묶음으로 가는 진입로 3종 브라우저 점검 → 결과와 조치는 **§13**
> 8차 이후: 2026-09-16 회화 교재 3권(`conversation/`)을 추가하며 홈 섹션·칩이 **53개**가 되었습니다. §13 의 "52"는 8차 측정 시점 값입니다(추가 내역은 `docs/content-roadmap.md` · `docs/conversation-research.md`).
> 9차 점검: 2026-09-16 · 방법: 정적 페이지 내비게이션(교재 pager · 과 이동 · 가이드 순차 이동)과 홈 「이어서 학습」을 실제 Chrome 으로 계측 → 결과와 조치는 **§14**
> 10차 점검: 2026-09-16 · 방법: 배포 노출(공개 주소 실측) · 서비스워커 프리캐시 대조 감사 · 정적 페이지 바로가기와 홈 깊은 구간 이동을 실제 Chrome 으로 계측 → 결과와 조치는 **§15**
> 13차 점검: 2026-09-18 · 방법: 채점 결과가 화면 낭독기에서 읽히는지(라이브 영역 0곳) · 정적 페이지 124개의 Tab 순서(건너뛰기 링크 0곳) · 문서·설정 대조 계측 → 결과와 조치는 **§18**
> 14차(후속): 2026-09-18 · 방법: 13차에서 남긴 두 권고(원본 스타일 분리 · 얇은 페이지 본문)를 실행하고 회귀 검사를 추가 → 결과와 조치는 **§19**
> 수치는 `npm run audit` 출력과 계측값 기준입니다.

<!-- audit:counts
pages = 128
sitemap_urls = 125
chapter_pages = 72
-->

---

## 0. 요약

| 심각도 | 건수 | 대표 항목 | 상태 |
| --- | --- | --- | --- |
| P1 (사용 흐름을 막음) | 3 | ① 문법 콘텐츠가 홈 42번째에 묻혀 있었음 ② 정적 페이지 다크 모드 부재 ③ 긴 홈에서 위로 돌아갈 길 없음 | ✅ 모두 수정 |
| P2 (불편·일관성) | 7 | 섹션 52개 탐색 부담, 정적 페이지→앱 복귀 동선, 라벨 중복, 터치 영역, 표 가로 넘침, 초점 표시 누락, 칩 순서 불일치 | ✅ 모두 수정 |
| P3 (개선 여지) | 4 | `prefers-reduced-motion`, 페이지 무게, 오답노트 오디오, 문항 수 고정 | ✅ 3건 수정 · 1건 권고 |

**이번 점검에서 수정 완료:** P1 전부, P2 전부, P3 중 모션 감소·오답노트 오디오·문항 수 선택.
**남은 권고:** 페이지 무게(분할) → 14차 후속에서 1단계(스타일 분리)를 실행했습니다(§19-1).

**2차 점검(2026-09-15):** P1 3건 · P2 4건 · P3 2건을 추가로 찾아 전부 수정했습니다 → §7.

**3차 점검(2026-09-15):** 2차에서 남긴 권고 5건(§7-9)을 모두 처리하고, 1차에서 미뤄 둔 모바일 필터 스크롤 단서까지 마무리했습니다 → §8.

**4차 점검(2026-09-15):** 3차에서 남긴 3건(이미지 최적화 · 최소화·분할 · 정적 페이지 브라우저 점검)을 처리했습니다 — 공유 카드 231KB → 94KB, 정적 페이지 공용 자산 분리(0.4MB 중복 제거), 작은 터치 영역 6종 수정 → §9.

**5차 점검(2026-09-15):** 프리렌더 확장(4 → 17개 컨테이너 · 413항목), 앱 화면 11개의 작은 터치 표적 20곳 수정, 배포 실주소 점검 경로 추가 → §10.

**6차 점검(2026-09-15):** 사용자 제보 3건을 고쳤습니다 — ① 모바일 햄버거 메뉴가 화면보다 길면 **아래 항목을 고를 수 없었음**(375px 에서 277px 잘림) ② 메뉴·안내 창을 열어 둔 채 **뒤로가기를 누르면 사이트를 떠났음** ③ 발음(TTS)이 **단어 앞의 유닛 이모지를 함께 읽었음**. 여기에 닫기 동작·스크롤 고정·초점·`lang="en"` 같은 사용성 보강을 더했습니다 → §11.

**7차 점검(2026-09-15):** 사용자 제보를 고쳤습니다 — **PC(브라우저 창을 줄인 데스크톱)에서 `⋯ 더 보기` 메뉴가 화면보다 길어져 아래 항목(진단·대시보드·가이드)을 고를 수 없었음**(1280×650 에서 18px · 1100×600 에서 68px 잘림). 6차에서 모바일 `☰` 패널에만 넣었던 높이 제한을 두 패널 공통 토큰으로 묶고, 창을 좁힐 때의 상태 정리까지 맞췄습니다 → §12.

**8차 점검(2026-09-16):** 1차부터 "섹션 52개 탐색 부담"으로, 2차부터 "페이지 무게"로 남아 있던 **긴 홈**을 목차 4묶음 단위 `<details>` 아코디언으로 접었습니다(모바일 87,785px → 44,411px · 데스크톱 43,132px → 19,090px). 그 과정에서 **목차의 4묶음과 본문 순서가 어긋나 있던 것**을 찾아 본문을 재배치했습니다 → §13.

---

## 1. P1 — 사용 흐름을 막는 문제 (모두 수정)

### 1-1. 문법 교재·문법 문제 풀이가 홈 42·43번째 섹션에 있었음 ✅

| | |
| --- | --- |
| **증상** | 홈은 `home-section` **52개**를 한 페이지에 쌓는 구조입니다. 새 문법 콘텐츠가 42·43번째에 있어, 칩을 47번째까지 옆으로 스크롤하거나 페이지를 40섹션 이상 내려야 닿을 수 있었습니다. |
| **조치** | `📐 오늘의 문법 팁`(4번째 섹션) 바로 뒤(5·6번째)로 이동. 문법 학습 동선이 **팁 → 교재 → 문제**로 이어집니다. |
| **검증** | 섹션 순서 계측: `… 4 오늘의 문법 팁 / 5 문법 교재 3단계 / 6 문법 문제 풀이 / 7 학습 모드` |

### 1-2. 정적 페이지에 다크 모드가 없음 ✅

| | |
| --- | --- |
| **증상** | 앱은 `toeic1000_theme` 으로 다크 테마를 지원했지만, 정적 페이지 47개는 흰 화면 고정이었습니다. |
| **조치** | ① 첫 페인트 전 `<html class="dark-pending">` 적용 인라인 스크립트 ② `:root` 변수를 `html.dark-pending` 에서 어둡게 재정의(배경·카드·본문·경고/정답 배경 등 19개 토큰) ③ 하드코딩 색을 변수로 치환. |
| **검증** | 생성 페이지 계측: `dark-pending` 2건(`units/unit-01.html`·`grammar/basic.html` 등 전 페이지 공통). 앱에서 고른 테마가 정적 페이지로 그대로 이어집니다. |

### 1-3. 긴 홈에서 위로 돌아갈 길이 없음 ✅

| | |
| --- | --- |
| **증상** | 홈이 52섹션(약 4만 px)인데 섹션 메뉴는 스크롤과 함께 사라지고(비고정), `맨 위로` 버튼도 없었습니다. 30번째 섹션쯤에서 목차로 돌아가려면 손가락으로 3만 px 를 되감아야 했습니다. |
| **조치** | 우하단 부동 `↑ 맨 위로` 버튼(`#toTop`) 추가. 700px 이상 스크롤하면 나타나고, `prefers-reduced-motion` 이면 애니메이션 없이 즉시 이동합니다. 인쇄 시 숨김. |
| **검증** | 마크업·스크롤 임계값(700px)·`reduce-motion` 분기 계측 확인. |

---

## 2. P2 — 불편·일관성 문제 (모두 수정)

### 2-1. 홈 한 페이지에 52개 섹션을 한 줄 칩 49개로 탐색 ✅

- **증상**: `.section-nav` 가 `overflow-x: auto` 라, 가로 스크롤이 있다는 단서 없이 49개 칩을 옆으로 밀어야 했습니다. 데스크톱(마우스)에서는 뒤쪽 칩 접근이 특히 어려웠습니다.
- **조치**: ① 자주 쓰는 8개만 위 줄에 노출 ② 나머지 44개는 `🗂 전체 목차` 버튼으로 **주제별 4묶음**(어휘 익히기 / 파트별 훈련 / 말하기·쓰기 / 실전·학습 관리)에 담아 접기 ③ 넓은 화면(≥900px)은 펼친 상태로 시작 ④ `aria-expanded`·`aria-controls` 로 접힘 상태 노출.
- **검증**: 섹션 52 = 칩 52(모두 연결) · 그룹 4개 · 토글 동작 · `npm run audit` 의 "섹션 메뉴 칩 52개(모두 연결됨)".

### 2-2. 정적 페이지에서 앱 학습으로 돌아오는 길이 없음 ✅

- **증상**: 교재 페이지 CTA 가 `홈` 하나뿐이고 앱은 해시를 해석하지 않아, "이 교재의 문제를 앱에서 풀기" 로 바로 연결할 수 없었습니다.
- **조치**: ① 앱에 해시 진입 지원(`goToHash` — `#grammar-quiz`·`#grammar-quiz-basic` … `#units`·`#dashboard` 등) ② `?level=basic#grammar-quiz` 형태로 **단계까지 지정해 바로 시작** ③ 교재 상단에 `✏️ 이 교재 문제 풀기` 버튼 배치.
- **검증**: 교재 → 앱 링크 계측(`../index.html?level=basic#grammar-quiz`), `applyLevelQuery()` + `hashchange` 리스너 확인.

### 2-3. 같은 화면에 이름이 비슷한 문법 진입점 3개 ✅

- **증상**: `📘 문법 가이드`(자료 허브의 탭) · `📘 문법 교재`(칩) · `✏️ 문법 문제`(칩)가 한 화면에 공존해 무엇이 다른지 구분되지 않았습니다.
- **조치**: 첫 번째를 목적지 이름인 **`📐 필수 문법 보기`** 로 변경.

### 2-4. 모바일 터치 영역이 44px 미만인 컨트롤 ✅

- **증상**: `.btn` 이 모바일에서 약 33px, `quiz-head select` 가 38px 이었습니다.
- **조치**: `select` → `min-height: 44px`, 모바일 `.btn` → `min-height: 40px`(프로젝트가 쓰던 "약 40px" 기준에 맞춤).
- **검증**: `@media (max-width: 640px)` 규칙 계측.

### 2-5. 문법 표가 좁은 화면에서 빽빽함 ✅

- **증상**: `.gtable` 3열이 375px 화면에서 한 열 약 100px 로 좁아 읽기 어려웠습니다.
- **조치**: 표를 `.gtable-wrap`(`overflow-x: auto`) 로 감싸고 `min-width: 340px` 를 주어, 좁은 화면에서는 표만 가로로 밀리게 했습니다. 520px 이하에서는 글자·여백을 한 단계 줄였습니다.

### 2-6. 많은 조작 요소에 키보드 초점 표시가 없음 ✅

- **증상**: `:focus-visible` 규칙이 `.btn`·`.lvl-btn`·`.quiz-opt` 등 6종에만 걸려 있어, **섹션 칩·토글·오답 보기(`.bank-opt`)·모드 버튼(`.mode-btn`)·유닛 버튼** 등은 키보드로 이동해도 어디에 초점이 있는지 보이지 않았습니다.
- **조치**: `a` · `button` · `summary` · `select` · `input` · `[tabindex]` 전역 `:focus-visible` 윤곽선(3px `--accent`)으로 통일.
- **검증**: 계측 — 접근 가능한 이름이 없는 버튼 0개, 라벨 없는 `select`/`input` 0개, 중복 `id` 0개.

### 2-7. 상단 칩 순서가 문서 순서와 어긋남 ✅

- **증상**: 첫 줄 칩이 `… 문법 문제(6번째 섹션) → 오늘의 대화문(23번째) → 학습 모드(7번째)` 순이어서, 대화문으로 갔다가 다시 위로 되돌아와야 했습니다.
- **조치**: `학습 모드 → 오늘의 대화문` 순으로 교체해 첫 줄이 `1·2·3·4·5·6·7·23` 로 단조 증가하게 했습니다.

---

## 3. P3 — 개선 여지

| 항목 | 내용 | 상태 |
| --- | --- | --- |
| 모션 감소 | 8곳의 `scrollIntoView({behavior:"smooth"})` 가 `prefers-reduced-motion` 을 무시했습니다 | ✅ `scrollToEl()` 헬퍼로 일원화 — 모션 감소 설정이면 `auto` 로 이동. `#toTop` 도 동일 |
| 오답노트 오디오 | 문법 오답노트가 문제·정답·해설만 보여 주고 🔊 가 없었습니다 | ✅ **빈칸을 정답으로 채운 완성 문장**을 낭독하는 버튼 추가 |
| 문항 수 선택 | 문법 문제가 8문항 고정이었습니다 | ✅ **5 / 8 / 10 / 20 / 전체** 선택 추가(`grammarCountSel`) |
| 페이지 무게 | `index.html` 약 409KB, `grammar/advanced.html` 약 84KB **(당시 값)** | ✅ 원본 스타일 분리로 628KB(−11% · §19-1), 배포본은 분리·최소화 적용. 남은 항목(인라인 앱 스크립트 302KB)은 `docs/app-split-plan.md` 2단계 |

---

## 4. 메인 페이지(`index.html`) 집중 점검

진입 → 탐색 → 학습 → 복습 4단계로 나눠 실제 조작 순서를 따라가며 확인했습니다.

### 4-1. 진입 (첫 화면)

| 확인 항목 | 결과 |
| --- | --- |
| 건너뛰기 링크 | ✅ `<a class="skip-link" href="#mainContent">본문으로 바로가기</a>` — 키보드 사용자가 상단바를 건너뛰고 본문으로 바로 이동 |
| 문서 구조 | ✅ `<html lang="ko">`, `h1` 1개(사이트명), 이후 섹션 제목은 `h2` 58개 — 제목 단계가 어긋나지 않음 |
| 첫 화면 정보 위계 | ✅ 배지 → 제목 → 부제 → 오늘의 응원 문구 → 누적 지표 4개 → 주요 CTA 3개(`오늘의 퀴즈`/`암기 카드`/`실력 진단`) 순으로 시선이 자연스럽게 내려감 |
| 뷰포트 | ✅ `width=device-width, initial-scale=1.0` |
| 모바일 글자 확대 | ✅ `@media (max-width: 640px)` 에서 입력·선택 글자를 16px 로 올려 iOS 사파리 자동 확대를 막음 |

### 4-2. 탐색 (섹션 이동)

| 확인 항목 | 결과 |
| --- | --- |
| 목차 규모 | 52섹션 → 첫 줄 8칩 + `🗂 전체 목차`(4묶음 44칩). 접기 덕분에 첫 화면이 칩으로 뒤덮이지 않음 |
| 현재 위치 표시 | ✅ 스크롤 위치를 감지해 해당 칩을 강조(`aria-current="page"`)하고, **접힌 묶음 안의 섹션이면 `지금: ○○` 텍스트로 보완**해 모바일에서도 위치를 알 수 있음 |
| 위로 가기 | ✅ `↑ 맨 위로` 부동 버튼(700px 이상 스크롤 시) |
| 해시 진입 | ✅ `#grammar-quiz-basic` 등 정적 페이지·외부 링크에서 특정 섹션으로 바로 진입 |
| 남은 의견 | 섹션 메뉴가 스크롤과 함께 사라지므로, 깊은 구간에서는 `맨 위로` 를 거쳐 다시 접근해야 함(스크롤 부담을 줄이려 비고정을 선택) |

### 4-3. 학습 (문제 풀이)

| 확인 항목 | 결과 |
| --- | --- |
| 조작 기반 | ✅ 223개 버튼 모두 `<button>` 기반 + 접근 가능한 이름 보유(계측 0건 누락) — 키보드만으로 풀이 가능 |
| 초점 표시 | ✅ 모든 버튼·링크·입력에 3px 윤곽선 |
| 터치 영역 | ✅ 모바일 `.btn` 40px · `select` 44px · `.quiz-opt` 46px |
| 문항 선택 | ✅ 단계(`grammarLevelSel`) × 문항 수(`grammarCountSel`) 조합 |
| 피드백 | ✅ 정답·오답 표시 + 해설 + 완성 문장 낭독 |
| 사이즈 | ⚠️ `index.html` 약 409KB **(당시 값 · 현재는 §18-1)** — 초기 파싱 비용. 교재·유닛 데이터는 `<script src>` 로 분리돼 있어 본문 파싱은 가벼움 |

### 4-4. 복습 (오답·기록)

| 확인 항목 | 결과 |
| --- | --- |
| 오답노트 | ✅ `toeic1000_grammarwrong` 에 문제·보기·정답·해설 저장, 맞히면 자동 제거, `📕 오답만 다시 풀기` |
| 낭독 | ✅ 완성 문장 🔊 버튼(빈칸을 정답으로 치환해 읽어 줌) |
| 개인정보 | ✅ 모든 기록은 localStorage 에만 저장 · 서버 전송 없음(개인정보처리방침과 일치) |
| 약점 연동 | ✅ 대시보드 `문법` 영역·약점 추천(`CATEGORY_DRILLS`)에 단계별 기록 연결 |

### 4-5. 남은 문제점 (수정하지 않고 남긴 것)

| 심각도 | 내용 | 이유 |
| --- | --- | --- |
| P3 | `index.html` 단일 파일(2차 점검 시점 465KB) | 데이터 분리·최소화는 빌드 구조 변경이 커서 별도 작업 필요. **2차에서 첫 화면의 숨은 목록 렌더만 제거**(§7-2) |
| P2(잠재) | `말하기·쓰기` 계열 섹션이 `말하기·쓰기 템플릿`·`Speaking·Writing 시험 구성`·`Speaking 유형별 연습`·`Writing 유형별 연습`·`말하기·쓰기 연습` **5개**로 이름이 겹침 | **2차에서 목차 묶음·칩 이름 정리**(§7-5·§7-6). 다만 5개 섹션을 하나로 합치는 콘텐츠 통합은 여전히 남음 |
| P3 | 섹션 메뉴가 비고정 | 고정 시 세로 공간을 상시 차지해 모바일 체감이 나쁠 수 있어 `맨 위로` 버튼으로 대체 |

---

## 5. 잘 되어 있는 점 (유지)

- 문항 카드·보기·피드백이 모두 `<button>` 기반이라 키보드만으로 풀이가 가능합니다.
- 영어 단어·발음기호·예문에 `lang="en"` 이 적용되어 화면 낭독기가 영어로 읽습니다(앱 렌더러 + 정적 페이지 공통).
- 정적 페이지의 🔊 버튼은 앱의 TTS 설정(`toeic1000_ttsvoice`·`ttslang`·`ttsrate`)을 공유해 목소리·속도 선택이 페이지를 넘어 이어집니다.
- 미지원 브라우저에서는 🔊 버튼을 숨기고(`hidden`), Esc 로 낭독을 멈출 수 있습니다.
- 새 문법 섹션은 정적 링크(`바로가기`)를 함께 노출해 JS 를 실행하지 않는 크롤러(네이버 Yeti)도 교재 3권을 발견할 수 있습니다.
- 오답노트를 포함한 모든 학습 기록은 localStorage 에만 저장되고 서버로 전송되지 않습니다.

---

## 6. 재점검 방법

```bash
npm run check        # 콘텐츠·문구·사이트 감사 + UI/TTS/문법 테스트 + 빌드
npm run audit        # 감사 3종만 실행
npm run test         # 홈 UI·TTS·문법 회귀 테스트만 실행
```

계측 스크립트(점검에 사용한 값):

```bash
# 홈 섹션·칩·그룹 개수, 칩-섹션 연결, 문서 순서 일치
node -e "const h=require('fs').readFileSync('index.html','utf8');\
const secs=[...h.matchAll(/class=\"home-section\"[^>]*aria-label=\"([^\"]*)\"/g)].map(m=>m[1]);\
const chips=[...h.matchAll(/class=\"sn-chip\" data-target=\"([^\"]*)\"/g)].map(m=>m[1]);\
console.log('섹션',secs.length,'칩',chips.length,'그룹',(h.match(/class=\"sn-group\"/g)||[]).length);\
console.log('연결 안 된 칩',chips.filter(c=>!secs.includes(c)).length);\
console.log('크기 KB',Math.round(h.length/1024));"

# 정적 페이지 다크 모드 적용 여부
grep -c 'prefers-color-scheme\|dark-pending' units/unit-01.html grammar/basic.html

# 모션 감소 대응 · 맨 위로 버튼
grep -c 'prefers-reduced-motion' index.html   # ≥ 1
grep -c 'id="toTop"' index.html               # 1
```

---

## 7. 2차 점검 (2026-09-15)

1차 점검은 "검색엔진·접근성·학습 흐름"을 봤고, 2차는 **첫 로드에 실제로 무슨 일이 일어나는지**(전송량·DOM 노드·테마 초기값)와 **목차 분류**를 계측했습니다.
`npm run audit` 는 1차 조치 이후 계속 통과하고 있었으므로 모두 감사가 잡지 못하는 항목입니다.

### 7-1. 요약

| 심각도 | 항목 | 상태 |
| --- | --- | --- |
| P1 | 목록이 아닌 화면(퀴즈·암기·시험 등)에 검색창·난이도 필터가 보이지만 눌러도 아무 일이 없었음 | ✅ 수정 |
| P1 | 첫 화면에서 보이지도 않는 단어 카드 1,000장(요소 약 1만 8천 개)을 즉시 렌더 | ✅ 수정 |
| P1 | 다크 모드가 OS 설정(`prefers-color-scheme`)을 무시 — 항상 밝게 시작 | ✅ 수정 |
| P2 | `Pretendard` 를 `font-family` 에만 쓰고 실제로는 로드하지 않았음 | ✅ 수정 |
| P2 | 목차 4묶음이 6·5·2·31개로 불균형하고 일부는 잘못된 묶음에, 일부는 문서 순서와 어긋남 | ✅ 수정 |
| P2 | 칩 라벨과 섹션 이름 불일치(같은 목차에 "Part 1" 이 두 개) | ✅ 수정 |
| P2 | 서비스워커가 배포 직후 새 HTML + 옛 데이터를 조합할 수 있었음 | ✅ 수정 |
| P3 | 위 문제들이 감사·테스트에 걸리지 않았음 | ✅ 감사·테스트 추가 |

### 7-2. 첫 화면에서 숨은 목록 1,000장을 만들고 있었음 ✅

| | |
| --- | --- |
| **증상** | 초기화가 `renderUnits("")` 로 끝난 뒤 `showHome()` 을 호출했습니다. 즉 홈을 띄우기 **전에** `display:none` 인 `#units` 안에 30유닛 × 1,000장 카드를 전부 만들었습니다. 카드 1장의 요소는 18개 — 합계 **약 1만 8천 개 노드**를 화면에 보이지도 않은 채 생성했습니다. |
| **조치** | ① 목록은 목록 화면(`showListView`)에 처음 들어갈 때 `ensureUnitsRendered()` 로 한 번만 그립니다. ② 진행률·예상 점수에 필요한 `totalCount` 는 `collectUnits()` 로 옮겨 목록을 그리기 전에도 값이 맞게 했습니다. ③ 홈의 "추천 학습 → 바로 가기"(`goUnit`)는 이미 그려진 목록이면 다시 그리지 않습니다. ④ 학습 기록 초기화·불러오기 뒤에는 `invalidateUnits()` 로 비워 두고 다음에 목록을 열 때 다시 그립니다. |
| **검증** | `tools/test-home-ux.mjs` — 목록을 처음 열 때 1회만 그리는지, 검색어를 그대로 넘기는지, 초기화 뒤 다시 그리는지 확인. 초기화 블록에 `renderUnits(` 직접 호출이 없는지도 검사합니다. |

### 7-3. 검색창이 결과를 보여 줄 수 없는 화면에도 떠 있었음 ✅

| | |
| --- | --- |
| **증상** | `showView()` 는 검색창을 `homeView` 에서만 숨겼습니다. 그래서 퀴즈·암기 카드·실전 시험·대시보드 화면에서도 검색창과 초급/중급/고급 필터가 보였는데, 결과가 들어가는 `#units` 는 그 화면에서 `display:none` 이라 **입력해도 화면이 바뀌지 않았습니다.** 사용자에게는 검색 기능이 고장 난 것으로 보입니다. |
| **조치** | 검색창은 단어 목록(`units`) 화면에서만 노출합니다. `showView()` 한 곳만 고쳐 모든 화면에 함께 적용됩니다. |
| **검증** | `tools/test-home-ux.mjs` — 홈·암기·퀴즈·시험 화면에서 `display:none`, 목록 화면에서만 보이는지 6건 확인. |

### 7-4. 다크 모드가 OS 설정을 무시 ✅

| | |
| --- | --- |
| **증상** | 앱·정적 페이지 모두 테마 초기값이 `localStorage.getItem("toeic1000_theme") \|\| "light"` 였고, 저장소 어디에도 `prefers-color-scheme` 가 없었습니다(문서에만 등장). OS 를 다크 모드로 쓰는 사용자는 첫 방문마다 흰 화면을 보고 🌙 을 직접 눌러야 했습니다. |
| **조치** | ① 첫 페인트 전 인라인 스크립트가 저장된 값이 없을 때만 OS 설정을 씁니다. ② `setTheme(t, persist)` 로 저장 여부를 분리해, OS 를 따르는 중에는 `localStorage` 에 쓰지 않습니다(이후 OS 설정이 바뀌면 그대로 따라갑니다). ③ 직접 고른 값이 있으면 언제나 그 값이 우선합니다. ④ 정적 페이지 생성기(`build-pages.mjs`)에도 같은 규칙을 넣고 48페이지를 재생성했습니다. |
| **검증** | `tools/test-home-ux.mjs` — (저장값 없음 + OS 다크) → 다크, (저장값 light + OS 다크) → 라이트, (저장값 dark + OS 라이트) → 다크를 앱과 정적 페이지(`units/unit-01.html`) 양쪽에서 확인. |

### 7-5. 본문 서체(Pretendard)를 선언만 하고 로드하지 않았음 ✅

| | |
| --- | --- |
| **증상** | `font-family: "Pretendard Variable", Pretendard, "Noto Sans KR", …` 가 앱과 정적 페이지 48곳에 있었지만 `@font-face` 도 CDN 링크도 없었습니다. Windows 는 Malgun Gothic, 안드로이드는 기본 고딕으로 떨어져 디자인의 자간·굵기 위계가 그대로 무너졌습니다. |
| **조치** | 공식 배포본 중 **dynamic subset**(필요한 글자 조각만 내려받는 방식) CSS 를 `preconnect` 와 함께 연결했습니다(`pretendardvariable-dynamic-subset.min.css`, 약 53KB · 조각당 약 30KB). 같은 출처가 아닌 서체는 서비스워커가 캐시 우선으로 처리해 오프라인에서도 같은 글꼴이 나옵니다. |
| **검증** | 앱·정적 페이지에 링크가 들어갔는지 `grep -c 'pretendard@v1.3.9'`… 생성 페이지까지 49곳 확인. CDN 응답 200 확인(woff2 조각 포함). |

### 7-6. 목차 4묶음이 불균형하고 일부가 잘못 분류돼 있었음 ✅

| | |
| --- | --- |
| **증상** | ① `말하기·쓰기` 묶음에 말하기·쓰기 콘텐츠가 하나도 없고(비즈니스 표현·스피드 어휘), 정작 말하기·쓰기 섹션 5개는 `실전·학습 관리` 에 있었습니다. ② 어휘 콘텐츠(단어 패밀리·최소대립쌍·어원 팁)가 `파트별 훈련` 에 있었습니다. ③ `실전·학습 관리` 에 31개(전체 60%)가 몰려 "묶음"의 의미가 사라졌습니다. ④ 그 묶음만 칩 순서가 `…45,46,47,51,52,48,49,50,8,24` 로 문서 순서와 어긋나, 칩을 따라가면 페이지를 위로 되감아야 했습니다. |
| **조치** | 묶음을 "무엇을 하는 콘텐츠인가" 기준으로 다시 나누고, **안에서는 홈 문서 순서대로** 두었습니다. |

| 묶음 | 이전 | 이후 |
| --- | --- | --- |
| 어휘 익히기 | 6개 | **12개** (단어 패밀리·최소대립쌍·어원 팁·스피드·동의어·연어·빈도 어휘가 합류) |
| 파트별 훈련 | 5개 | **11개** (Part 문제은행·복수 지문·Part 6·치환·연어·전치사·LC 심화·받아쓰기) |
| 말하기·쓰기 | 2개 | **6개** (S&W 템플릿·S&W 구성·스피킹·라이팅·확장 연습) |
| 실전·학습 관리 | 31개 | **15개** (빠른 도구·리포트·특징·자료 허브 외) |

| | |
| --- | --- |
| **검증** | `audit-site.mjs` 에 **묶음 검사**를 추가했습니다 — ① 모든 칩이 실제 섹션을 가리키는지 ② 메뉴에서 닿을 수 없는 섹션이 없는지 ③ **묶음 안 칩이 문서 순서대로인지**. 순서를 일부러 뒤집어 감사가 실패하는 것까지 확인했습니다. 계측값: `어휘 익히기 12 · 파트별 훈련 11 · 말하기·쓰기 6 · 실전·학습 관리 15`, 4묶음 모두 문서 순서 일치. |

### 7-7. 칩 라벨과 섹션 이름이 어긋났음 ✅

| 칩(이전) | 실제 섹션 | 조치 |
| --- | --- | --- |
| `🖼 Part 1` | LC 심화 훈련 (제목: "LC 심화: Part 1 묘사 · 숫자·금액") | `🖼 LC 심화` — 같은 목차의 `🎧 LC 1·2` 와 구분 |
| `🗣 템플릿` | 말하기·쓰기 템플릿 | `🗒️ S&W 템플릿` |
| `🗣️ 말하기·쓰기` | 말하기·쓰기 연습 (제목: "말하기·쓰기 확장 연습") | `🗣️ S&W 확장 연습` |
| `🔗 연어` / `🔗 연결어` | 동의어·반의어·연어 학습 / 연결어·접속부사 훈련 | `🔗 동의어·연어` / `🔀 연결어` (아이콘 중복 제거) |
| `📈 빈도 어휘` | 빈도순 기출 어휘 | `🔥 빈도 어휘` (`📈 리포트` 와 아이콘 중복 제거) |

### 7-8. 서비스워커가 새 HTML + 옛 데이터를 조합할 수 있었음 ✅

| | |
| --- | --- |
| **증상** | HTML 은 네트워크 우선인데 `data/*.js` 는 stale-while-revalidate 라, 배포 직후 첫 로드에서 **새 `index.html` + 캐시된 옛 데이터**가 섞일 수 있었습니다. 캐시 이름(`toeic-monster-v6`)을 손으로 올려야 하는 구조였습니다. |
| **조치** | 캐시 이름을 `v7` 로 올려 배포 시 이전 캐시를 정리하고, 새로 추가한 외부 서체(CDN) 요청은 캐시 우선으로 담아 오프라인에서도 같은 글꼴이 나오게 했습니다. |
| **검증** | `node --check sw.js` · `npm run check`. |

### 7-9. 남은 권고 (이번에 수정하지 않음)

| 심각도 | 내용 | 이유 |
| --- | --- | --- |
| P2(UI) | 상단바에 컨트롤이 18개(액션 11 · TTS 5 · 테마 · 메뉴)라 1024~1280px 구간에서 두 줄로 접힙니다. 같은 기능이 히어로 CTA·학습 모드 그리드와 3중으로 겹칩니다. | 헤더 구조 개편이라 별도 작업 필요 |
| P2 | iOS 홈 화면 아이콘이 적용되지 않습니다(`apple-touch-icon` 이 SVG, manifest 도 SVG 만). | PNG 아이콘 생성이 필요한 별도 작업 |
| P3 | 첫 로드 1,004KB(raw) / 322KB(gzip), 동기 `<script src>` 38개, 최소화 없음, `data/extra.js` 167KB 상시 로드 | 빌드·분할 구조 변경이 큼 |
| P3 | 홈의 52개 섹션 콘텐츠가 JS 로만 그려져 비-JS 크롤러(네이버 Yeti)에는 보이지 않습니다. | 프리렌더 확장은 별도 작업 |
| P3 | `404.html` 이 없습니다. | Pages 기본 404 로 동작 |

→ 위 5건은 3차 점검(**§8**)에서 모두 처리했습니다.

### 7-10. 2차 점검 재현 방법

```bash
npm run check      # 감사 3종 + 홈 UI·TTS·문법 테스트 + 빌드 (모두 통과해야 함)

# 첫 화면이 숨은 목록을 그리지 않는지 (초기화 블록에 renderUnits 직접 호출이 없어야 함)
node -e "const h=require('fs').readFileSync('index.html','utf8');\
const s=h.indexOf('  collectUnits();');const e=h.indexOf('  showHome();');\
console.log('초기화 블록의 renderUnits 호출:',(h.slice(s,e).match(/renderUnits\\(/g)||[]).length);"

# 목차 묶음 구성과 문서 순서 일치
node tools/audit-site.mjs | grep '섹션 메뉴 묶음'

# 본문 서체·테마 기본값이 앱과 정적 페이지에 함께 들어갔는지
grep -c 'pretendard@v1.3.9' index.html units/unit-01.html grammar/basic.html
grep -c 'prefers-color-scheme' index.html units/unit-01.html
```

---

## 8. 3차 점검 (2026-09-15)

2차에서 **남은 권고 5건(§7-9)** 을 모두 처리하고, 실제 브라우저로 다시 재어 보며 2건을 더 찾아 고쳤습니다.

### 8-1. 요약

| 항목 | 이전 | 이후 |
| --- | --- | --- |
| 상단바 컨트롤 (1100px) | 18개 · 139px · **2줄** | 핵심 7개 + ⋯ 더 보기 9개 · **79px · 1줄** |
| 설치형 앱 아이콘 | SVG 1개 (iOS·설치 배너에서 무시됨) | SVG + PNG 192/512/maskable + `apple-touch-icon` |
| 동기 `<script src>` | 38개 | **0개** (34개 `defer` + 외부 스크립트 1개 `async`) |
| 첫 로드 | 1,009KB raw / 327KB gzip | **947KB raw / 294KB gzip** (`data/extra.js` 167KB 지연 로드) |
| 홈 비-JS 콘텐츠 | 없음 (빈 화면) | 프리렌더 256항목 + `<noscript>` 목차(52주제 · 정적 링크 5개) |
| 404 | GitHub Pages 기본 404 | `404.html` (noindex · 루트 절대 경로) |
| 모바일 필터 스크롤 단서 | 없음 (355px 숨은 채로 스크롤) | 오른쪽 끝 페이드 + 스크롤 스냅 |
| 375px 가로 넘침 | 16px (30일 계획 카드) | **0px** |

검증은 `npm run check:ci` 에 더해 **실제 브라우저 계측**(`npm run check:browser`, §8-9)으로 했습니다 — 자바스크립트 오류 0건 · 실패한 요청 0건.

### 8-2. 상단바에 컨트롤이 18개라 1024~1280px 에서 두 줄로 접혔음 ✅

| | |
| --- | --- |
| **증상** | 한 줄에 `h1 + 진행률 + TTS 5개 + 테마 + 액션 11개` 가 들어 있었습니다. 1100px 실측 **139px(2줄)** — sticky 헤더라 화면을 그만큼 더 먹고, 같은 기능이 홈의 히어로 CTA·학습 모드 그리드와 3중으로 겹쳤습니다. |
| **조치** | 헤더에는 늘 쓰는 것만 남기고 나머지를 `⋯ 더 보기` 패널로 옮겼습니다. 밖: `🔊 ⏹ 🌙`(빠른 설정 3개) + `🏠 홈 · 📖 목록 · 🃏 암기 · 🎯 퀴즈`. 패널 안: 발음 목소리·속도·도움말 + `⏱ 시험 · 🔁 복습 · 🎧 리스닝 · 📝 모의고사 · 🧭 진단 · 📊 대시보드 · 📘 가이드`. |
| **검증** | 1100px **79px·1줄**, 375px 120px(햄버거로 접힘 — 이전과 같은 높이). `tools/check-browser.mjs` 가 "상단바가 몇 줄인가"와 "⋯ 더 보기 패널이 열리고 · 바깥을 누르면 닫히는지"를 계속 감시합니다. |
| **부수 수정** | 액션이 사라진 `.topbar-actions` 에 붙어 있던 `shareBtn` 바인딩을 옮겼습니다. 그대로 두면 그 줄에서 스크립트가 멈춰 뒤쪽 초기화가 전부 실행되지 않았습니다. |

### 8-3. iOS 홈 화면 아이콘이 적용되지 않았음 ✅

| | |
| --- | --- |
| **증상** | `apple-touch-icon` 이 SVG 였고 manifest 아이콘도 SVG 뿐이었습니다. iOS 는 SVG 아이콘을 쓰지 않으며, Chrome 설치 요건도 192/512 래스터 아이콘을 요구합니다. |
| **조치** | `tools/make-icons.py`(Pillow, `make_workbook.py` 선례)로 `icon-192.png` · `icon-512.png` · `icon-maskable-512.png` · `apple-touch-icon.png` 를 생성하고, manifest 와 `<head>` 에 연결했습니다. 서비스워커(v8)가 오프라인용으로 함께 담습니다. |
| **검증** | `audit-site.mjs` 에 아이콘 검사 추가 — manifest 아이콘 파일 존재·PNG 포함 여부·`apple-touch-icon` 이 SVG 로 되돌아가지 않았는지. |

### 8-4. 첫 로드가 1,004KB · 동기 스크립트 38개였음 ✅

| | |
| --- | --- |
| **증상** | 어휘 스크립트 34개가 `defer` 없이 파싱을 막았고, 홈 확장 섹션에만 쓰는 `data/extra.js`(167KB)를 첫 화면에서 항상 내려받았습니다. |
| **조치** | 데이터 스크립트 34개를 `defer` 로 바꾸고(초기화를 `DOMContentLoaded`→`startApp` 으로 이동), `extra.js` 는 `requestIdleCallback`(또는 그 내용이 필요한 시점)에 받도록 했습니다. |
| **검증** | **1,009KB → 947KB raw**, **327KB → 294KB gzip**. 동기 `<script src>` 0개. 두 번째 방문부터는 서비스워커 캐시에서 즉시 로드됩니다. |

### 8-5. 홈이 비-JS 크롤러·비-JS 사용자에게 빈 화면이었음 ✅

| | |
| --- | --- |
| **증상** | 홈 52개 섹션 대부분이 JS 로 그려져, 네이버 Yeti 같은 크롤러와 자바스크립트를 끈 사용자에게는 제목만 보였습니다. |
| **조치** | `tools/prerender-home.mjs` 신설 — **앱의 렌더 함수를 그대로 `vm` 으로 실행**해 나온 HTML 을 같은 컨테이너에 심습니다(혼동 어휘 20 · 어근·접두사 24 · 단어 패밀리 12 · 빈도순 기출 200 = **256항목**). 함께 `<noscript>` 목차(52개 주제 + 자바스크립트 없이 볼 수 있는 정적 페이지 5개)를 넣었습니다. |
| **검증** | `--check` 로 최신 여부를 확인하고 `npm run verify` 에 물렸습니다. `audit-site.mjs` 는 표시(마커)가 사라지거나 내용이 비면 실패시킵니다. |
| **비용** | `index.html` 470KB → 576KB raw(gzip 141KB). 첫 로드 **총량**은 `extra.js` 를 미룬 만큼 줄었고(§8-4), 앱이 뜨면 같은 내용으로 다시 그려져 중복되지 않습니다. |

### 8-6. 깨진 주소가 GitHub Pages 기본 404 로 떨어졌음 ✅

| | |
| --- | --- |
| **조치** | `tools/build-pages.mjs` 가 `404.html` 을 생성합니다. **중요:** 이 파일은 임의의 주소에서도 응답되므로 상대 경로(`./units/`)를 쓰면 `/units/foo` 요청 시 `/units/units/` 를 가리킵니다. 그래서 안의 링크를 전부 **루트 절대 경로**로 적었습니다. |
| **검증** | `audit-site.mjs` 가 ① 파일 존재 ② `robots=noindex` ③ 상대 경로 링크 0개 를 검사하고, 사이트맵에는 들어가지 않습니다. `verify-generated.mjs` 대상에도 추가했습니다. |

### 8-7. 실제 브라우저로 재어 보며 새로 찾은 2건 ✅

| | |
| --- | --- |
| **모바일 난이도 필터의 숨은 가로 스크롤** | 375px 에서 필터 줄이 355px 넘게 화면 밖에 있는데, 모바일에서는 스크롤 막대가 보이지 않아 **넘긴다는 사실 자체를 알 수 없었습니다**. 오른쪽 끝을 흐리게(mask) 해 "더 있다"는 신호를 주고, 버튼에 스크롤 스냅을 걸었습니다. |
| **30일 학습 계획 카드가 16px 넘침** | `.sprint-item` 이 고정 폭 칸 4개를 한 줄에 넣어 375px 에서 **문서 전체가 가로로 16px 밀렸습니다**(스와이프할 때마다 화면이 흔들림). 좁은 화면에서는 줄을 바꾸고, 길이 제한이 있는 계획 이름은 말줄임으로 줄였습니다. |

두 건 모두 `npm run check:browser` 가 잡아냈고, 고친 뒤 0px 을 확인했습니다.

### 8-8. 남은 권고

| 심각도 | 내용 | 이유 |
| --- | --- | --- |
| P3 | 최소화·분할이 없습니다. `index.html` 이 576KB(gzip 141KB)이고, 어휘 데이터도 파일 34개로 나뉘어 있습니다. | 빌드 파이프라인(번들러) 도입이 필요해 이번 범위를 넘습니다. 프리렌더·지연 로드로 체감은 개선했습니다. |
| P3 | `og-image.png` 가 231KB 입니다(공유 카드 1장). | ✅ 4차 점검(§9-2)에서 94KB 로 줄였습니다. |
| — | 암기 카드·퀴즈·시험은 자바스크립트가 있어야 동작합니다. | 설계된 동작 — 대신 홈의 읽을거리와 정적 학습 자료는 JS 없이도 전부 남겼습니다. |

→ 최소화·분할(첫 줄)은 4차 점검(§9-3·§9-5)에서 **생성물에 적용**했습니다.

### 8-9. 3차 점검 재현 방법

```bash
npm run check:ci      # 빌드 → 감사 3종 → 테스트 3종 → 생성물·프리렌더 최신성 (모두 통과해야 함)
npm run check:browser # 실제 헤드리스 Chrome 계측 (Chrome 이 없으면 건너뜀)

# 프리렌더가 데이터와 어긋나지 않았는지 (고치려면 npm run prerender)
node tools/prerender-home.mjs --check

# 상단바 컨트롤 수 · 첫 화면 카드 수 · 프리렌더 표시
grep -c '<script defer src=' index.html                       # 34 (동기 script src 는 0개)
node -e "const h=require('fs').readFileSync('index.html','utf8');\
console.log('프리렌더 표시:',(h.match(/prerender:start/g)||[]).length,'· noscript:',h.includes('<noscript>'))"

# 브라우저 계측이 보는 값 (오류 0 · 1줄 헤더 · 가로 넘침 0)
npm run check:browser | tail -20
```

---

## 9. 4차 점검 (2026-09-15) — 전송량과 정적 페이지

3차에서 남긴 3가지(이미지 최적화 · 최소화·분할 · 정적 페이지 브라우저 점검)를 처리했습니다.

### 9-1. 요약

| 항목 | 이전 | 이후 |
| --- | --- | --- |
| 공유 카드 `og-image.png` | 231KB | **94KB** (무손실 재압축 · 픽셀 동일) |
| 정적 페이지 스타일 | 페이지마다 9KB 인라인 (48곳 = 0.4MB 중복) | **`assets/site.css` 8.9KB 한 파일** (gzip 2.3KB) |
| 문법 교재 예문 듣기 스크립트 | 페이지마다 2.3KB 인라인 (3곳) | **`assets/speak.js` 3.2KB** (gzip 1.6KB · defer) |
| 정적 페이지 전송량(대표 9개) | 281KB raw / 73KB gzip | **198KB raw / 51KB gzip** |
| 예문 듣기 버튼(🔊) | 22×22px | **28×28px** (터치 목표 · WCAG 2.5.8) |
| 정답 보기 토글 | 높이 21px | **높이 45px** |
| 이전·다음 · 푸터 · 빵부스러기 링크 | 높이 15~17px | **높이 25~44px** |

범위: 정적 페이지 10개 × (1100px · 375px) × (오류 · 가로 넘침 · 스타일 적용 · 히트 영역)을
실제 브라우저로 확인했고, 홈 페이지 점검과 함께 `npm run check:browser` 한 번으로 돌아갑니다.

### 9-2. 공유 카드가 231KB 였음 ✅

| | |
| --- | --- |
| **증상** | 카톡·슬랙·X·디스코드가 공유 카드를 받아 가는데, 파일이 231KB 였습니다. 브라우저 캡처로 만든 PNG 는 압축이 느슨합니다. |
| **조치** | `tools/make-og-image.py` 를 추가해 **픽셀을 바꾸지 않고** 다시 압축했습니다(231KB → 94KB, 58% 감소 · 다이어트 후 다시 읽어 원본과 픽셀 비교). 웹폰트(Pretendard)가 이미지 안에 글자로 들어가 있어도 그대로입니다. |
| **검증** | `python tools/make-og-image.py --check` 가 "더 줄일 수 있음"을 잡고, `audit-site.mjs` 가 160KB 초과를 문제로 보고합니다(재캡처 후 재압축을 잊는 사고 방지). |

### 9-3. 같은 스타일을 48번 · 같은 스크립트를 3번 다시 받고 있었음 ✅

| | |
| --- | --- |
| **증상** | 정적 페이지 48개가 **같은 9KB 스타일을 각자 인라인**으로 갖고 있었습니다(중복 0.4MB). 문법 교재 3권은 예문 듣기 스크립트 2.3KB 도 각자 들고 있었습니다. |
| **조치** | 생성기(`build-pages.mjs`)가 `assets/site.css`(주석·공백 정리 후)와 `assets/speak.js` 를 만들고, 페이지는 `<link>`·`defer <script>` 로 참조합니다. 같은 파일을 여러 페이지가 받아 쓰므로 브라우저·서비스워커 캐시가 한 번만 받습니다. |
| **검증** | 대표 9페이지 합계 281KB → 198KB(raw), 73KB → 51KB(gzip). `audit-site.mjs` 가 “정적 페이지에 2KB 넘는 인라인 style”과 “듣기 버튼이 있는데 speak.js 가 없는 페이지”를 문제로 잡습니다. `verify-generated.mjs` 대상에 `assets/` 를 넣어 최신 여부를 검사합니다. |
| **캐시 주의** | 인라인일 때는 HTML 과 함께 항상 새 스타일이 왔지만, 파일로 빼면 브라우저·서비스워커 캐시를 거칩니다. 그래서 서비스워커가 설치 때 두 파일을 함께 담고, 배포할 때마다 캐시 이름을 올려(이 항목을 쓴 시점 v9 · **현재 `sw.js` 는 v11**) 이전 버전이 남지 않게 했습니다. |

### 9-4. 정적 페이지를 처음으로 실제 브라우저로 재어 봤음 ✅

| | |
| --- | --- |
| **점검** | 단어장 허브 · 유닛 페이지 · 숙어 · 문법 허브 · 문법 교재 · 한 장 요약 · 가이드 허브 · 가이드 · 개인정보 · 404 — 1100px·375px 에서 JS 오류·실패한 요청·가로 넘침·스타일 적용·히트 영역을 확인했습니다. |
| **결과** | 넘침·오류는 0건이었고, **손가락 히트 영역**이 실제 문제였습니다: 예문 듣기 버튼 22×22px, 예문 듣기 버튼이 달린 문장 안의 아이콘, 정답 보기 토글 21px, 이전·다음과 푸터 링크 15px. |
| **조치** | 아이콘 버튼 22→28px, 정답 보기 토글 높이 21→45px, 이전·다음 44px, 푸터·빵부스러기 링크 25px. 기준은 두 단계(주요 컨트롤 44px · 그 밖의 링크 24px=WCAG 2.5.8 AA)이고, 문장 속 인라인 링크는 규격의 예외라 건너뜁니다. |

### 9-5. `index.html` 은 왜 최소화하지 않았나 (판단 기록)

`index.html` 은 **손으로 고치는 소스이자 그대로 배포되는 산출물**입니다(586KB, gzip 141KB: 인라인 `<style>` 70KB + 앱 스크립트 279KB).
최소화하려면 `dist/` 를 만들어 배포 경로를 바꿔야 하는데, 그 변경은 GitHub Pages 설정과 CI 배포 방식까지 함께 바꾸는 일이라 이번 범위를 넘습니다.

대신 **생성물은 최소화했습니다**: 정적 페이지의 공용 CSS 를 별도 파일로 굽고(§9-3) 주석·공백을 정리했으며, 홈도 프리렌더(§8-5)·스크립트 지연(§8-4)으로 첫 화면 비용을 줄였습니다.
어휘 데이터 34개 파일을 1개로 묶는 것도 검토했지만, 저장소에 원본 292KB 가 그대로 남은 채 묶음 파일이 하나 더 생기고 줄어드는 것은 요청 수뿐이라(HTTP/2 에서 왕복 비용이 작음) 하지 않았습니다.

### 9-6. 남은 권고

| 심각도 | 내용 | 이유 |
| --- | --- | --- |
| P3 | `index.html` 자체의 최소화·분할은 여전히 없습니다. | 배포 경로(`dist/`) 재구성이 필요해 별도 결정 사항입니다(§9-5). |
| P3 | 정적 페이지 HTML 자체는 압축하지 않았습니다(들여쓰기 유지). | 문장 사이 공백을 지우면 글이 붙어 버려, gzip 이득(1~2%)보다 위험이 큽니다. |

### 9-7. 4차 점검 재현 방법

```bash
npm run check:ci       # 빌드 → 감사 3종 → 테스트 3종 → 생성물·프리렌더·공용 자산 최신성
npm run check:browser  # 홈 + 정적 페이지 10개를 실제 헤드리스 Chrome 으로 계측

# 공유 카드가 최적인지 (고치려면 python tools/make-og-image.py)
python tools/make-og-image.py --check

# 정적 페이지가 공용 스타일을 쓰는지 (인라인 style 이 2KB 넘으면 감사가 실패)
grep -l 'assets/site.css' units/*.html grammar/*.html guides/*.html 404.html | wc -l   # 48
node tools/audit-site.mjs | grep -E '공용 스타일|공유 카드'
```

---

## 10. 5차 점검 (2026-09-15) — 프리렌더 확장과 터치 영역, 배포 실주소

### 10-1. 요약

| # | 문제 | 심각도 | 상태 |
| --- | --- | --- | --- |
| 10-2 | 홈에서 프리렌더한 것은 **어휘 목록 4개(256항목)** 뿐이고, 문법·독해·템플릿·가이드 같은 **읽는 콘텐츠**는 여전히 JS 로만 그려졌습니다. | P2 | ✅ |
| 10-3 | 앱 **화면 11개**를 처음으로 실제 브라우저로 넘겨 보니, 손가락 표적이 작은 곳이 **20곳** 있었습니다(주로 학습 필터·템플릿 탭·발음 버튼). | P2 | ✅ |
| 10-4 | 배포본(`https://toeic.monster`)을 실주소로 점검하는 경로가 없어, "로컬은 통과인데 배포본은 옛 버전"인 상태를 알아챌 수 없었습니다. | P3 | ✅ |

### 10-2. 홈 프리렌더가 "읽는 콘텐츠"를 비워 두고 있었음 ✅

§8-5 에서 프리렌더를 넣었지만 대상은 **어휘 목록**(혼동 어휘·어근·단어 패밀리·빈도순)에 한정돼 있었습니다.
즉 네이버 Yeti 나 자바스크립트를 끈 사용자에게 홈은 여전히 "단어 목록만 있는 페이지"였고, 정작 **체류 시간을 만드는 글**(문법 팁·리딩 지문·이메일 템플릿·30일 계획)은 비어 있었습니다.

각 섹션의 "JS 없이 읽히는 글자 수"를 재어 대상을 정하고, 컨테이너가 있고 데이터가 정적인 섹션을 전부 넣었습니다.

| | 이전 | 이후 |
| --- | --- | --- |
| 프리렌더 컨테이너 | 4개 (어휘) | **17개** (14개 묶음) |
| 프리렌더 항목 | 256항목 | **413항목** |
| JS 없이 읽히는 글자 | (어휘 목록) | **45,551자** |

추가한 곳: 동의어·반의어 26 · 리딩 미니 지문 10 · Part 7 복수 지문 1 · 이메일·회의 템플릿 11 · 말하기·쓰기 템플릿 12 · S&W 시험 구성·연습 26 · 빈출 구동사·숙어 12 · 전략·공략 가이드 9 · 30일 스프린트 30.

**"앱과 어긋나지 않게" 유지하는 방법**은 그대로입니다 — `tools/prerender-home.mjs` 가 **앱의 렌더 함수를 `vm` 으로 그대로 실행**해 나온 HTML 을 심고, `--check` 가 데이터와 한 글자라도 다르면 실패시킵니다(`npm run verify` 에 연결).
`audit-site.mjs` 도 홈 프리렌더 섹션 수·`<noscript>` 링크 수·읽히는 글자 수를 함께 보고하며, 마커가 사라지면 실패합니다.

### 10-3. 앱 화면 11곳의 손가락 표적이 작았음 ✅

`tools/check-browser.mjs` 는 상단바 버튼을 눌러 **화면을 실제로 옮겨 가며** 홈·목록·암기·퀴즈·시험·복습·리스닝·모의고사·진단·대시보드·가이드 11개 화면을 375px 로 계측합니다(자바스크립트 오류 · 가로 넘침 · 히트 영역).

첫 실행에서 **20곳**이 걸렸습니다. 기준은 두 단계입니다 — **주요 컨트롤 44px**(모바일 HIG·Material), **문장 사이 아이콘 버튼 24px**(WCAG 2.5.8 AA). 문장 흐름에 섞이는 인라인 링크는 규격의 예외라 제외합니다.

| 요소 | 이전 | 이후 |
| --- | --- | --- |
| 홈 섹션 메뉴 칩 `.sn-chip` | 32px 높이 | **44px** |
| 전체 목차 토글 `#snToggle` | 33px 높이 | **44px** |
| 난이도 필터 `.lvl-btn` | 35px 높이 | **44px** |
| 가이드 탭 `.guide-tab` | 35px 높이 | **44px** |
| 모바일 `.btn` · `.topbar .btn` | 40px 높이 | **44px** |
| 닫기 `.modal-close` `#guideClose` | 30×40 | **44×44** |
| 문장 속 발음 버튼 `.ex-speak` | 20×20 | **24×24** |
| 앱 푸터 링크 `.footer-links a` | 11×21 ("홈") | **가로 25px · 세로 28px** |
| 정적 페이지 푸터 링크 `footer.ft a` | 11px 폭 | **가로 27px** (48개 페이지 공통) |

세로 여백만 주면 "홈"처럼 **한 글자 링크는 폭이 11px** 로 남아 옆 링크를 잘못 짚습니다. 그래서 세로(높이)와 가로(좌우 패딩)를 **둘 다** 보장하도록 고쳤고, 실패 메시지에도 어떤 요소인지(`a "홈" 11×31`)가 나오도록 `check-browser.mjs` 의 이름 표시에 **요소 텍스트**를 붙였습니다.

고친 뒤 20곳 → **0곳**입니다.

### 10-4. 배포본을 실주소로 점검하는 길이 없었음 ✅

로컬 검사만으로는 "배포된 것이 어느 버전인지"를 알 수 없습니다. `node tools/check-browser.mjs --live` 를 추가해 **배포된 사이트를 그대로** 열어 봅니다.

| 확인 | 결과 |
| --- | --- |
| `robots.txt` 가 사이트맵을 알려 주는가 | ✓ |
| 사이트맵 48개 주소가 전부 응답하는가 (404 면 색인에서 밀림) | ✓ |
| 없는 주소가 **404 상태 + 우리 404 페이지**로 응답하는가 | ✓ |
| 공용 자산(`site.css`·`speak.js`·아이콘 3종)이 배포본에 있는가 (푸시 누락 감지) | ✓ |
| 홈이 압축되어 내려오는가 | ✓ gzip **157KB** |

이 모드로 배포본을 재어 보니, 위 10-3 의 20곳이 **배포본에도 그대로** 있었습니다(배포본은 당시 `9ca91fa`).
즉 이번 수정이 반영되지 않은 상태를 실주소에서 바로 확인할 수 있었습니다.
푸시(`5331ef5`) 뒤 같은 명령을 다시 돌려 **앱 화면 11개 포함 전 항목 통과**를 확인했습니다
(추가된 프리렌더 본문만큼 배포본 홈 전송량은 gzip 142KB → 157KB).

### 10-5. 남은 권고

| 심각도 | 내용 | 이유 |
| --- | --- | --- |
| P3 | `index.html` 최소화·`dist/` 분리(§9-5), 정적 페이지 HTML 압축(§9-6) | 배포 경로 결정이 필요한 별도 작업입니다. |
| P3 | 홈 프리렌더가 늘어난 만큼 `index.html` 이 커졌습니다(**577KB → 665KB** raw · gzip 140KB → **155KB**). | 자바스크립트가 켜진 방문자에게는 이 90KB 가 "나중에 어차피 다시 그릴 내용"이라 순수한 추가 비용입니다. 그럼에도 **비-JS 크롤러·사용자에게 읽히는 글자 45,551자**를 얻는 교환으로 판단했습니다. 줄이려면 프리렌더 본문을 별도 파일로 빼서 지연로딩해야 하는데, 그러면 SEO 상 이점이 사라집니다(크롤러는 지연 로딩을 기다리지 않음). |

### 10-6. 5차 점검 재현 방법

```bash
npm run check:ci                     # 빌드 → 감사 3종 → 테스트 3종 → 생성물·프리렌더 최신성
npm run check:browser                # 홈 + 정적 10p + **앱 화면 11개**를 실제 Chrome 으로 계측
node tools/check-browser.mjs --live  # 배포본(toeic.monster) 상태·자산·색인 점검

# 프리렌더가 어디까지 들어갔는지 (섹션 · 항목 수 · 바이트)
node tools/prerender-home.mjs --check
node tools/audit-site.mjs | grep -E '프리렌더|읽히는 글자'

# 히트 영역 기준을 일부러 깨뜨려 검사가 잡는지 확인
#   index.html 의 `min-height: 44px` 중 하나를 `32px` 로 바꾸고 npm run check:browser
```

---

## 11. 6차 점검 (2026-09-15) — 모바일 메뉴 스크롤·뒤로가기·낭독 이모지

사용자가 직접 겪은 3건을 신고 순서대로 재현하고 고쳤습니다.
셋 다 **모바일에서 학습을 막는 문제**였습니다.

### 11-1. 요약

| # | 문제 | 심각도 | 상태 |
| --- | --- | --- | --- |
| 11-2 | 모바일 햄버거 메뉴가 화면보다 길어지면 **아래 항목이 잘려 고를 수 없었음** (375px 에서 277px 잘림) | P1 | ✅ |
| 11-3 | 메뉴·안내 창을 열어 둔 채 **뒤로가기를 누르면 사이트를 떠났음** ("닫기"가 아니라 "이탈") | P1 | ✅ |
| 11-4 | 발음(TTS)이 **단어 앞의 유닛 이모지를 함께 읽었음** (`💼company`) — 단어 카드 1,000장 전부 | P1 | ✅ |
| 11-5 | 화면 전환 스크롤이 `prefers-reduced-motion` 을 무시, 영어 학습 내용의 `lang` 미지정, 안내 창 뒤 화면 스크롤 | P3 | ✅ |

### 11-2. 모바일 햄버거 메뉴가 길어지면 아래 항목을 고를 수 없었음 ✅

| | |
| --- | --- |
| **증상** | 1023px 이하에서 `☰` 를 누르면 내비게이션 전체(발음 설정 + 시험·복습·리스닝·모의고사·진단·대시보드·가이드·공유)가 드롭다운 하나로 접힙니다. 패널에는 높이 제한도 `overflow` 도 없어서(절대 위치 + `display:flex`), 화면보다 길어지면 **그냥 잘렸습니다.** 스크롤할 수단이 없어 아래 항목은 영영 누를 수 없었습니다. |
| **측정** | 375×720 에서 패널이 뷰포트보다 **277px** 길었습니다(마지막 항목이 화면 밖). |
| **조치** | ① 패널에 `max-height: min(74vh, calc(100dvh - 112px))` + `overflow-y: auto` + `overscroll-behavior: contain` — 메뉴 **안에서** 스크롤합니다. `dvh` 는 주소창이 접히는 모바일 브라우저에서 실제 보이는 높이를 쓰고, 모르는 브라우저는 `100vh` 로 대체됩니다. ② 열려 있는 동안 뒤 화면이 따라 움직이지 않도록 `body.menu-open { overflow: hidden }`. ③ 펼친 메뉴에서 지금 화면을 왼쪽 띠(`--accent` inset)로 표시. |
| **검증** | `npm run check:browser` — 패널이 뷰포트 안에 들어오고, 아래로 끝까지 스크롤하면 마지막 항목이 보이며, 가로 넘침 0px. |

### 11-3. 뒤로가기가 "닫기"가 아니라 "사이트 떠나기"였음 ✅

| | |
| --- | --- |
| **증상** | 메뉴를 열거나 안내 창을 띄운 뒤 휴대폰·브라우저의 **뒤로가기**를 누르면, 열린 것을 닫지 않고 곧바로 이전 페이지로 나갔습니다. 학습 중 가장 흔한 제스처라 학습 흐름이 끊겼습니다. |
| **조치** | 열 때 `history.pushState` 로 **항목 하나를 빌려** 두고, `popstate`(뒤로가기)가 오면 메뉴·안내 창을 닫습니다. 주소는 그대로라 화면은 바뀌지 않습니다. 직접 닫을 때(✕·Esc·바깥 누름·항목 선택)는 빌린 항목을 스스로 되돌려놓아, 뒤로가기 기록이 쌓이지 않습니다. 중첩(메뉴 → 안내 창)은 **개수(`guardDepth`)** 로 세어, 창이 남아 있으면 빌린 항목을 놓지 않습니다. |
| **범위** | 모바일 `☰` 메뉴, 데스크톱 `⋯ 더 보기`, `📘 TOEIC 시험 안내`, `❔ 목소리 도움말` — 네 곳 모두 같은 규칙입니다. |
| **보강** | 열면 첫 항목으로 초점 이동, 닫으면 `☰` 버튼으로 초점 복귀(항목을 골라 닫힐 때는 그대로 둠 — 시선이 이동한 화면으로 가야 하므로), Esc 로 닫기, 화면이 1024px 이상으로 넓어지면 자동 닫기. |
| **검증** | `tools/test-home-ux.mjs` §6 이 가짜 브라우저에서 열기→뒤로가기→Esc→항목 선택→화면 넓어짐을 모두 실행해 확인합니다. `check-browser.mjs` 는 실제 Chrome 에서 `history.back()` 뒤에도 주소가 그대로이고 패널·창만 닫히는지, **메뉴 → 안내 창으로 이어진 뒤에도 뒤로가기 한 번이면 창만 닫히는지** 봅니다. |

### 11-4. 발음(TTS)이 단어 앞의 이모지를 함께 읽었음 ✅

| | |
| --- | --- |
| **증상** | 단어 카드의 🔊 를 누르면 `💼company` 처럼 **단어 앞의 유닛 아이콘까지 읽었습니다.** |
| **원인** | 단어 칸이 `<div class="w" lang="en"><span class="unit-emoji">💼</span>company</div>` 구조인데, 낭독은 화면 글자(`textContent`)를 그대로 읽고 있었습니다. 유닛 아이콘이 붙은 카드가 **1,000장 전부**라 모든 발음이 해당됐습니다. |
| **조치** | ① 낭독할 때는 아이콘이 없는 원래 단어(`data-word`, 예: `company`)를 씁니다. ② 그래도 다른 경로(화면에만 있는 장식 기호·데이터에 섞인 이모지)로 새지 않도록 `ttsClean()`·`normSpeak()` 이 이모지·딩벳·변형 선택자를 **소리에서 제외**합니다(`🔊 ✈️ ✅ ⭐` 등 + 서로게이트 쌍 그림 이모지). ③ 정적 페이지가 쓰는 `assets/speak.js` 에도 같은 규칙을 넣었습니다. ④ `audit-text.mjs` 는 이제 **낭독 대상에 이모지가 섞이면 실패**시켜, 다음에 같은 일이 생기면 데이터 단계에서 잡힙니다. |
| **검증** | `tools/test-tts-voice.mjs` §7 이 `ttsClean` 을 그대로 추출해 8가지를 확인하고(단어 앞·문장 중간·변형 선택자 이모지, 화살표 `→`/`~`/빈칸은 기존 동작 유지), `check-browser.mjs` 는 실제 Chrome 에서 `speechSynthesis.speak` 를 가로채 **낭독 문자열이 `company` 이고 이모지가 없음**을 확인합니다. |

### 11-5. 그 밖의 사용성 보강 ✅

| 항목 | 내용 |
| --- | --- |
| 모션 감소 | 화면을 옮길 때의 맨 위로 스크롤이 `prefers-reduced-motion` 을 무시했습니다(다른 이동은 이미 `scrollToEl` 로 처리). `showView()` 도 같은 규칙을 쓰게 했습니다. |
| 화면 낭독기 | 영어 단어·예문에 `lang="en"` 이 카드에는 있었지만 다른 곳에는 없었습니다 → 오늘의 단어·명언·문법 팁·숙어·혼동 어휘·어원·빈도 어휘·퀴즈 예문·암기 카드에 함께 넣어, 한국어 음성이 영어를 어색하게 읽지 않게 했습니다. |
| 안내 창 | 창이 열려 있는 동안 뒤 화면이 함께 스크롤되던 문제를 `body.modal-open` 으로 막았습니다. |
| 검사 확장 | `tools/test-home-ux.mjs` §6(메뉴 17건) · `test-tts-voice.mjs` §7(낭독 11건) · `check-browser.mjs`(메뉴·뒤로가기·낭독·`lang` 10항목) 를 추가했습니다. |

### 11-6. 남은 권고

| 심각도 | 내용 | 이유 |
| --- | --- | --- |
| P3 | `index.html` 최소화·`dist/` 분리(§9-5), 정적 페이지 HTML 압축(§9-6) | 배포 경로 결정이 필요한 별도 작업입니다. |
| P3 | 화면 전환(홈·목록·퀴즈 …)은 아직 브라우저 기록에 남지 않습니다 — 뒤로가기는 메뉴·창을 닫는 데까지만 씁니다. | 화면 전환이 기록에 쌓이면 "목록 → 단어 → 목록"에서 뒤로가기가 여러 번 필요해져, 학습 흐름이 더 불편해질 수 있습니다(해시 딥링크와의 충돌도 함께 설계해야 하는 별도 작업). |

### 11-7. 6차 점검 재현 방법

```bash
npm run check:ci       # 빌드 → 감사 3종 → 테스트 3종 → 생성물·프리렌더 최신성
npm run check:browser  # 메뉴 스크롤·뒤로가기·낭독 이모지·lang 계측 포함

# 모바일 메뉴가 스크롤되는지 · 뒤로가기로 닫히는지 (실제 Chrome)
node tools/check-browser.mjs | grep -E '☰ 메뉴|마지막 항목|뒤로가기|이모지'

# 낭독 문장 다듬기 단위 테스트 (이모지 · 화살표 · 빈칸)
node tools/test-tts-voice.mjs | sed -n '/\[7\]/,$p'

# 낭독 대상에 이모지가 섞였는지 (섞이면 감사가 실패)
npm run audit:text
```

---

## 12. 7차 점검 (2026-09-15) — PC 에서 상단바 메뉴가 스크롤되지 않던 문제

사용자가 직접 겪은 1건(PC 환경에서 햄버거 메뉴가 스크롤되지 않음)을 재현하고 고쳤습니다.
같이 "PC 에서 쓰는 사람" 관점으로 전 화면을 다시 계측했고, 추가로 막히는 곳은 없었습니다.

### 12-1. 요약

| # | 문제 | 심각도 | 상태 |
| --- | --- | --- | --- |
| 12-2 | PC(창 높이 650px 이하)에서 `⋯ 더 보기` 패널이 화면 밖으로 잘려 **아래 항목을 고를 수 없었음** | P1 | ✅ |
| 12-3 | `⋯` 패널을 열어 둔 채 창을 좁히면 패널이 안 보이는 채 "열림"으로 남아, 빌린 뒤로가기 항목을 붙들고 있었음 | P3 | ✅ |
| 12-4 | PC 관점 전면 재점검(데스크톱 폭 4종 × 앱 화면 5개 · 좁은 창 7종) — 새로 막는 문제 없음 | — | ✅ |

### 12-2. PC 에서 `⋯ 더 보기` 메뉴가 스크롤되지 않았음 ✅

| | |
| --- | --- |
| **증상** | 데스크톱(1024px 이상)에서는 `☰` 대신 `⋯ 더 보기` 가 나머지 9개 항목(발음 설정 3 + 시험·복습·리스닝·모의고사·진단·대시보드·가이드)을 담습니다. 그 패널은 높이가 약 602px 인데 **높이 제한도 `overflow` 도 없었습니다.** 상단바는 고정(sticky)이라 패널이 페이지 스크롤을 따라 붙어 다니므로, 창이 낮으면 아래 항목은 페이지를 내려도 **영영 누를 수 없었습니다**("PC에서 햄버거 메뉴가 스크롤되지 않는다"는 제보). |
| **측정** | 헤드리스 Chrome 계측 — 1280×650: 패널 아래 끝 **668px**(화면 650px) · `overflow-y: visible` · `max-height: none` · 스크롤 가능 높이 **0px**. 1100×600·1024×600 도 같았고, 마지막 항목(📘 가이드)의 아래 끝은 **657px** 였습니다. 창을 줄이지 않은 1440×900 은 내용이 다 들어가 문제가 드러나지 않았습니다. |
| **원인** | 6차 점검(§11-2)에서 모바일 `☰` 패널에만 `max-height`+`overflow-y: auto` 를 넣고, 데스크톱 `⋯` 패널은 그대로 두었습니다. 두 패널이 같은 구조인데도 높이 규칙을 따로 가지고 있어 한쪽만 고쳐졌습니다. |
| **조치** | ① 두 패널이 **같은 높이 토큰**을 쓰도록 `:root { --menu-panel-max-h: min(74vh, calc(100dvh - 128px)) }` 로 묶었습니다(`dvh` 를 모르는 브라우저를 위해 `calc(100vh - …)` 대체값도 함께). ② 데스크톱 `⋯` 패널에 `max-height: var(--menu-panel-max-h)` · `overflow-y: auto` · `overscroll-behavior: contain` · `scrollbar-width: thin` 을 적용해 **패널 안에서** 스크롤되게 했습니다. |
| **검증** | 계측 — 1280×650: 패널 아래 끝 **545px**(< 650) · 스크롤 123px → 끝까지 내리면 마지막 항목이 패널·화면 안에 들어옴. 1440×900: 스크롤 0px(내용이 다 들어가 불필요한 스크롤막대가 생기지 않음). `tools/test-home-ux.mjs` §6 은 두 패널이 같은 토큰을 쓰는지(2곳)와 `overflow-y: auto` 여부를, `check-browser.mjs` §3-2c 는 실제 Chrome 1280×640 에서 패널이 화면 안에 들어오고 끝까지 스크롤되는지를 봅니다. |

### 12-3. `⋯` 패널을 열어 둔 채 창을 좁히면 상태가 남았음 ✅

| | |
| --- | --- |
| **증상** | 넓은 창에서 `⋯` 패널을 열고 창을 1023px 이하로 좁히면, 패널 내용은 `☰` 메뉴 안으로 합쳐져 **안 보이는데 "열림" 상태로 남았습니다.** 그 상태에서는 6차에 넣은 뒤로가기 항목을 계속 붙들고 있어(한 번 눌러도 아무 일이 없음) 학습 중 뒤로가기가 한 번 무효가 됐습니다. |
| **조치** | 창 크기가 바뀌어 드롭다운이 **사라지는 쪽**으로 넘어가면 정리하도록 대칭을 맞췄습니다 — 넓어지면 `☰` 닫기(6차), 좁아지면 `⋯` 닫기(이번). |
| **검증** | `tools/test-home-ux.mjs` §6 이 가짜 브라우저에서 두 방향(1280 → 900 · 375 → 1280)을 모두 실행해 확인합니다. |

### 12-4. PC 관점 전면 재점검 (새로 막는 곳 없음)

| 항목 | 결과 |
| --- | --- |
| 상단바 줄 수 | 1024 · 1280 · 1440 · 1920px 모두 **1줄(79px)** — 1024~1280 구간에서 두 줄로 접히지 않습니다(고정 상단바가 세로 공간을 먹지 않음). |
| 가로 넘침 | 홈 · 목록 · 퀴즈 · 암기 · 대시보드 5개 화면 × 4개 폭 = 넘침 **0px**. |
| 가로 스크롤 단서 | 641~1023px(창을 좁힌 PC)에서 난이도 필터가 넘치지 않았고(0px), 넘치기 시작하는 640px 이하에서는 mask 단서가 켜집니다 — **단서 없이 넘치는 영역 0곳**. |
| 눌리는 요소 | 마우스 기준 최소치(WCAG 2.5.8 · 24×24)를 모두 넘습니다(상단바 버튼 높이 33px · 가장 작은 버튼 29px). 44px 기준은 터치 화면(640px 이하) 규칙 그대로입니다. 커서가 `pointer` 가 아닌 조작 요소는 0곳(시각적으로 숨긴 라벨·설명 문단 제외). |
| 아이콘 버튼 안내 | 상단바 🔊 · 🌙 · ⏹, 단어 카드 🔊 · ⭐ · ✅ 에 `title`/`aria-label` 이 있어 마우스 사용자도 뜻을 확인할 수 있습니다. |
| 같은 구조의 다른 패널 | 화면에 뜨는 패널은 `☰` · `⋯` · 안내 창뿐입니다. 안내 창은 이미 `max-height: 85vh` + `overflow-y: auto` 라 같은 문제가 없습니다(그 밖의 `position: absolute` 요소는 배지·아이콘 등 장식뿐). |
| 나머지 검사 | `npm run check:ci`(빌드 · 감사 3종 · 테스트 3종 · 생성물·프리렌더 최신성)와 `npm run check:browser`(정적 10p · 앱 11화면 · 다크 모드 · 뒤로가기 · 낭독) 전부 통과. |

### 12-5. 남은 권고

| 심각도 | 내용 | 이유 |
| --- | --- | --- |
| P3 | 데스크톱에서 지금 보고 있는 화면이 패널 **안** 항목일 때(시험·복습·리스닝·모의고사·진단·대시보드) 접힌 `⋯` 버튼에는 표시가 없습니다. | 패널을 열면 현재 항목이 `active` 로 보이고 화면 자체에도 제목이 있어 진행에는 지장이 없습니다. 접힌 버튼에 점을 찍는 방식이 후보지만, 요청 없는 시각 변경이라 이번에는 넣지 않았습니다(모바일 `☰` 도 같은 상태). |
| P3 | §11-6 에 남긴 2건(`index.html` 최소화·`dist/` 분리, 화면 전환의 브라우저 기록) | 배포 경로·해시 딥링크 설계가 함께 필요한 별도 작업입니다. |

### 12-6. 7차 점검 재현 방법

```bash
npm run check:ci       # 빌드 → 감사 3종 → 테스트 3종 → 생성물·프리렌더 최신성
npm run check:browser  # PC 낮은 창(1280×640) ⋯ 패널 · 좁은 창(900×640) ☰ 메뉴 계측 포함

# 창이 낮을 때 상단바 메뉴가 화면 안에서 스크롤되는지 (실제 Chrome)
node tools/check-browser.mjs | grep -E '⋯ 패널|☰ 메뉴|마지막 항목'

# 두 드롭다운이 같은 높이 토큰을 쓰는지 (CSS 회귀)
node tools/test-home-ux.mjs | sed -n '/\[6\]/,$p'

# 히트 영역 기준을 일부러 깨뜨려 검사가 잡는지 확인
#   index.html 의 --menu-panel-max-h 중 하나를 지우고 npm run test
```

---

## 13. 8차 점검 (2026-09-16) — 긴 홈을 목차 4묶음 단위로 접기

1차 점검부터 "섹션 52개 탐색 부담"으로, 2차부터 "페이지 무게"로 계속 남아 있던 **긴 홈**을 이번에 손봤습니다.
요청은 "더 보기·아코디언 같은 걸 쓰면 어떻겠나"였고, 검토 뒤 **목차가 이미 쓰고 있는 4묶음 단위로 `<details>` 로 접는** 쪽으로 정했습니다.
재배치·계측은 `tools/measure-home.mjs` 로 했습니다.

### 13-1. 요약

| # | 문제 | 심각도 | 상태 |
| --- | --- | --- | --- |
| 13-2 | 홈이 52섹션을 한 줄로 쌓아, 모바일에서 **87,785px**(약 117화면)를 손가락으로 내려야 했음 | P2 | ✅ 44,411px(−49%) |
| 13-3 | 목차는 4묶음으로 나뉘어 있는데 **본문은 그 순서가 아니어서**, 목차가 지도 노릇을 못 했음 | P2 | ✅ 본문을 묶음 순서로 재배치 |
| 13-4 | 접기만 하면 접힌 곳으로 가는 길(칩 · 주소 해시 · 인쇄)이 끊김 | — | ✅ 세 진입로 모두 보강 |
| 13-5 | 재배치로 첫 화면에 들어온 `.dialogue-speak` 이 히트 영역 검사에 걸림(원래 화면 밖에 숨어 있었음) | P3 | ✅ 기준 목록에 반영 |

### 13-2. 홈이 52섹션 · 모바일 87,785px 였음 ✅

| | |
| --- | --- |
| **증상** | 홈은 `home-section` **52개**(요약·배지·모드·유닛·구동사·어휘 심화·파트별 훈련·말하기·쓰기·실전 도구·FAQ …)를 한 페이지에 쌓는 구조입니다. 1차 점검(§1-3)에서 "홈이 52섹션(약 4만 px)"로 적어 둔 그대로였고, 좁은 화면에서는 섹션마다 카드가 한 줄로 풀려 훨씬 길어집니다. |
| **측정** | 헤드리스 Chrome 계측(문서 전체 스크롤 높이) — **375px: 87,785px**(약 117화면) · **1100px: 43,132px**(약 53화면). 섹션 높이 합계는 39,880px 로 문서의 92% 를 차지했고, 그중 상위 6개(문제은행 4,360 · 빈도순 기출 3,626 · 혼동 어휘 2,090 · 말하기·쓰기 템플릿 2,080 · 30일 스프린트 1,798 · 어원 팁 1,645)만 **15,599px(39%)** 였습니다. |
| **판단** | "더 보기"가 아니라 `<details>` 를 골랐습니다. 더 보기는 자바스크립트가 내용을 지웠다 다시 그리는 방식이라 ① 자바스크립트를 실행하지 않는 크롤러(네이버 Yeti)가 읽지 못하고 ② 브라우저 찾기(Ctrl+F)와 ③ 인쇄에서 사라집니다. 이 사이트는 `tools/prerender-home.mjs` 로 홈 콘텐츠를 미리 심어 검색 유입을 만들고, `audit-site.mjs` §3-6 이 그 자리가 채워져 있는지(프리렌더 마커 6종 · 각 200자 이상)를 계속 검사합니다 — SEO 콘텐츠 섹션에 더 보기를 적용하면 **감사가 바로 실패**합니다. `<details>` 는 접힌 내용도 DOM·HTML 소스에 그대로 남아 이 셋을 모두 지킵니다. |
| **조치** | 목차의 4묶음(어휘 익히기 · 파트별 훈련 · 말하기·쓰기 · 실전·학습 관리)을 본문에도 그대로 적용해 각각 `<details class="home-group">` 로 감쌌습니다. 자주 쓰는 8개(오늘의 학습 · 배지 · 오늘의 단어 · 문법 팁 · 문법 교재 · 문법 문제 · 학습 모드 · 대화문)는 접지 않고 위에 그대로 둡니다. 52개 = 앞줄 8 + **12/11/6/15** 이고, 기본값은 첫 묶음(📚 어휘 익히기)만 펼침 — 나머지 32개 섹션이 접힌 상태로 시작합니다. |
| **검증** | 계측 — **375px: 87,785px → 44,411px(−43,374px · −49%)** · **1100px: 43,132px → 19,090px(−24,042px · −56%)**. 참고로 네 묶음을 전부 접으면 375px 9,902px · 1100px 6,053px 입니다(기본값을 정할 때의 근거). 검색·인쇄 쪽은 그대로입니다 — 프리렌더 17개 유지, 자바스크립트 없이 읽히는 글자 **45,551자 → 45,629자**(묶음 제목 4줄이 늘어난 만큼), 파일 크기 693,517 → 698,607바이트(+0.7%). `npm run audit` 의 "홈 섹션 52개 · 섹션 메뉴 칩 52개(모두 연결됨)" 도 그대로입니다. |

### 13-3. 목차의 4묶음과 본문 순서가 어긋나 있었음 ✅

| | |
| --- | --- |
| **증상** | 목차 칩은 "무엇을 하는 콘텐츠인가" 기준으로 4묶음으로 나뉘어 있는데, **본문은 그 순서로 쌓여 있지 않았습니다.** 기능이 나중에 덧붙으면서 뒤에 이어 붙인 흔적입니다. 그래서 `4묶음 단위로 접기`를 하려 해도 문서 순서대로는 만들 수 없었습니다 — 묶음별로 이어지는 구간을 세면 **19조각**으로 흩어져 있었습니다. |
| **측정** | 순서 대조 예 — `8위 빠른 학습 도구`(실전·학습 관리) → `9~14위 어휘 6개`(어휘 익히기) → `15~16위 Part 1·2 · Part 3·4`(파트별 훈련) → `17위 단어 패밀리`(**어휘 익히기로 되돌아감**) → `20위 비즈니스 이메일`(말하기·쓰기) → `21위 스피드 어휘`(어휘 익히기) → `22위 주간 리포트`(실전·학습 관리) → `27위 문제은행`(파트별 훈련) → `28위 문맥·연어`(어휘 익히기) → `36위 빈도순 기출`(어휘 익히기) → `37위 30일 스프린트`(실전·학습 관리) → `45위 말하기·쓰기 연습`(말하기·쓰기). 코드 주석은 "칩을 따라 내려가면 페이지도 위에서 아래로 내려가도록"이라고 적혀 있었지만 실제로는 지켜지지 않고 있었습니다. |
| **조치** | 순서를 손으로 옮기지 않고 **목차(`.section-nav`)에서 읽었습니다** — 목차가 곧 정답이므로 어긋날 여지를 없앴습니다. 앞줄 8칩 + 4묶음 순서대로 52개 섹션을 재배치하고, 그 위에 `<details>` 를 씌웠습니다. 목차 자체(칩·묶음)는 손대지 않았습니다. |
| **검증** | 재배치 전/후 **섹션 집합이 같은지**(52개 · 중복 없음 · 누락 없음)와 칩↔섹션 1:1 을 스크립트가 확인한 뒤에만 파일을 썼습니다(같지 않으면 중단). 결과는 `npm run audit` — 섹션 52개 · 칩 52개 · 묶음 12/11/6/15 가 모두 연결됨. 부수 효과로, **칩을 따라 내려가면 페이지도 위에서 아래로 내려갑니다**(원래 의도대로). 참고로 `noscript` 목차는 본문 순서를 따라가므로, 재배치 뒤 `npm run verify` 가 어긋남을 알려 `tools/prerender-home.mjs` 를 다시 실행했습니다. |

### 13-4. 접힌 곳으로 가는 진입로 3종을 뚫었음 ✅

| | |
| --- | --- |
| **증상(막을 뻔한 것)** | 접기만 하면 "접힌 곳으로 가는 길"이 끊깁니다 — ① 섹션 메뉴 칩을 눌러도 높이가 0인 곳으로 스크롤되어 **화면이 비어 보이고** ② 정적 페이지·교재에서 `#해시` 로 들어와도 묶음이 닫힌 채라 역시 빈 화면이며 ③ 인쇄하면 화면에서 접어 둔 학습 내용이 **종이에서 통째로 빠집니다**(인쇄본은 "한 장으로 보여 주는 자료"로도 쓰입니다). |
| **조치** | ① **`scrollToEl` 한 곳**에서 조상 `<details>` 를 먼저 엽니다 — 칩 · 해시 링크 · 퀴즈 시작 등 섹션으로 가는 길목이 모두 이 함수를 지나므로 한 곳만 막으면 됩니다. ② 인쇄는 `beforeprint` 에서 접힌 묶음을 모두 펼치고 `afterprint` 에서 되돌립니다(CSS 로는 `<details>` 를 열 수 없습니다). 인쇄물에는 묶음 제목줄을 넣지 않고 왼쪽 띠 장식도 뺍니다. |
| **검증** | `node tools/measure-home.mjs` 가 실제 Chrome 에서 6건을 확인합니다 — 칩을 찾음 · **칩을 누르면 접힌 묶음이 먼저 펼쳐짐** · 펼친 뒤 해당 섹션이 화면에 들어옴 · **주소 해시로 들어와도 펼쳐짐** · 인쇄 전 모두 펼침(2개 → 0개) · 인쇄 뒤 원래대로(0개 → 2개). 375px·1100px 두 폭에서 통과. |

### 13-5. 재배치가 화면 밖에 숨어 있던 작은 버튼을 드러냈음 ✅

| | |
| --- | --- |
| **증상** | 재배치 뒤 `npm run check:browser` 가 `앱 홈 화면: .dialogue-speak "🔊" 24×24 (필요 44)` 로 실패했습니다. 원인이 재배치라는 점이 중요합니다 — 이 검사는 **첫 화면(뷰포트 안)에 있는 요소만** 보는데, 대화문 섹션이 `23위 → 8위` 로 올라오면서 그동안 화면 밖에 숨어 있던 버튼이 처음으로 검사 범위에 들어온 것입니다. 같은 코드가 몇 차례 점검을 통과해 온 이유가 이것입니다. |
| **조치** | `.dialogue-speak` 은 대화문 한 줄 끝에 붙는 인라인 🔊 라, 예문의 `.gex-speak`(28px)와 같은 성격입니다. 검사기의 `SMALL_ICONS` 예외 목록에 이 둘 중 `.gex-speak` 만 들어 있어 빠져 있던 것을 채웠습니다(24px 최소 기준은 이미 지킵니다). |
| **검증** | `npm run check:browser` 재통과(앱 11화면 · 정적 10페이지). **남은 판단:** 손가락으로 자주 누르는 자리라 44px 로 키우는 쪽이 낫다면 그렇게 바꾸는 것이 맞습니다 — 이번에는 프로젝트가 정한 "문장 흐름 속 아이콘은 24px" 정책을 따랐습니다. |

### 13-6. 남은 권고

| 심각도 | 내용 | 이유 |
| --- | --- | --- |
| P3 | **파일 무게는 그대로입니다**(`index.html` 698,607바이트 · 최소화·`dist/` 분할 미적용). | 이번에 줄인 것은 **스크롤 길이**이고 전송량이 아닙니다. §8-8 · §9-6 에서 미룬 배포 경로 재구성이 그대로 필요합니다(별도 결정 사항). |
| P3 | **📚 어휘 익히기 묶음이 기본 펼침이라 여전히 가장 깁니다** — 모바일 44,411px 중 대부분. | 이 묶음은 이 사이트의 본체(단어·어원·빈도 어휘)라 접으면 첫 화면이 비어 보입니다. 묶음 안에서 다시 접거나(섹션별), 긴 섹션을 나누거나, 콘텐츠를 합치는 쪽이 다음 후보입니다. |
| P3 | 묶음 펼침 상태가 저장되지 않아, 화면을 옮겼다 돌아오면 기본값으로 돌아갑니다. | 테마(`toeic1000_theme`)처럼 저장할 수 있지만, "접힌 걸 펼쳐 놓고 왔는데 다시 접혀 있다"와 "펼쳐 놓은 게 그대로다" 중 무엇이 덜 놀라운지는 취향 문제라 넣지 않았습니다. |
| P3 | 말하기·쓰기 계열 5개 섹션의 **콘텐츠 통합**(§7-5 에서 남긴 항목)은 그대로입니다. | 이번에는 순서와 접기만 다뤘습니다. 통합하면 길이를 클릭 없이 줄일 수 있어, 접기보다 효과가 큽니다. |

### 13-7. 8차 점검 재현 방법

```bash
npm run check:ci       # 빌드 → 감사 3종 → 테스트 3종 → 생성물·프리렌더 최신성
npm run check:browser  # 앱 11화면 · 정적 10페이지(히트 영역 · 넘침 · 오류)

# 홈 길이와 섹션별 기여도 (실제 Chrome)
node tools/measure-home.mjs                 # 1100px
node tools/measure-home.mjs --width 375     # 모바일 폭
node tools/measure-home.mjs --all-closed    # 네 묶음을 전부 접었을 때 높이까지

# 묶음 동작(칩 · 해시 · 인쇄)만 따로 보기 — 6건이 ✓ 인지 확인
node tools/measure-home.mjs | sed -n '/묶음 동작 점검/,$p'

# 목차와 본문이 1:1 인지 (묶음을 합치거나 이름을 바꾸면 여기서 걸립니다)
node tools/audit-site.mjs | grep -E '홈 섹션|섹션 메뉴 묶음'
```

---

## 14. 9차 점검 (2026-09-16) — 내비게이션

> 계기: “사용자 편의를 위한 네비게이션을 추가하면 어떨까” 라는 질문.
> 먼저 **이미 있는 것**을 셌습니다 — 홈은 53개 섹션 칩(모바일은 상위 8개 + `🗂 전체 목차`)에 스크롤 위치를 따라
> 현재 섹션을 강조하고(`aria-current` + `지금: …`), 정적 페이지는 상단 breadcrumb(+`BreadcrumbList`)·하단 pager·푸터 전역 메뉴를
> 갖추고 있었습니다. 그래서 **새로 얹는 대신 실제로 걸리는 곳**을 고쳤습니다.

### 14-1. 요약

| 심각도 | 내용 | 상태 |
| --- | --- | --- |
| P2 | 교재 pager 가 단계명을 두 번 찍음 (**「중급 중급 영문법 →」**) | ✅ 수정 |
| P2 | 교재를 읽는 중에는 이동 수단이 없음 — 12과 77KB 페이지에서 위 목차까지 8~10화면을 되돌아가야 함 | ✅ 수정 |
| P3 | 가이드 9편에 이전/다음 편 이동이 없음(유닛·교재에는 있음) | ✅ 수정 |
| P3 | 홈에서 “어디서 이어서 하지”를 찾아 헤맸음(마지막 학습 위치를 기억하지 않음) | ✅ 수정 |
| P2 | 교재의 “이 과”를 가리키는 주소가 없었음(12과가 한 문서) — 검색·공유로 특정 과를 열 수 없음 | ✅ 수정 |
| P2 | 낱개 과 페이지 → 앱 문제 풀이로 이어지는 길이 없었음(과를 고르라고 다시 시킴) | ✅ 수정 |
| P2 | 배포 주소와 어긋난 `canonical`·중복 `<title>` 을 잡는 감사가 없었음 | ✅ 수정 |
| P3 | 펼쳐 둔 홈 묶음이 화면을 옮겼다 돌아오면 기본값으로 돌아감 | ✅ 수정 |

### 14-2. 교재 pager 의 단계명 중복 ✅

교재 데이터는 `level: "중급"` + `title: "중급 영문법"` 처럼 **단계명이 이미 제목 안에** 들어 있는데,
`tools/build-pages.mjs` pager 가 `${level} ${title}` 로 이어 붙여 `중급 중급 영문법`·`고급 고급 영어회화` 가 나왔습니다(6페이지 8곳).
제목만 쓰도록 고쳤고, 교재 허브 JSON-LD 의 `name` 에도 같은 중복이 있어 함께 정리했습니다.

### 14-3. 교재 과 단위 이동 ✅

한 페이지에 12과가 이어지고 페이지가 77KB까지 커져, 과를 읽다가 위 목차로 돌아가는 수고가 컸습니다(과 이동 링크 **0개**).

- **과 끝 이동 링크**(`.chnav`): `↑ 목차`(새 `#toc` 앵커) + `다음 과 →`. 자바스크립트가 없어도 동작하는 기본 수단이고,
  회화·문법 6권 **72개 과** 전부에 붙습니다.
- **현재 과 바**(`#chBar`): 목차를 지나 과로 들어가면 화면 아래에 현재 과(`07과 조동사 기초`)와 `🗂 목차` · `다음 과` 가 따라붙습니다.
  페이지 끝 pager 가 보이면 겹치지 않게 내려갑니다. 인쇄에서는 숨기고, 자바스크립트가 없으면 나타나지 않습니다(링크가 그 역할을 대신).

### 14-4. 가이드 이전/다음 편 이동 ✅

가이드 9편은 순서대로 읽는 코스인데 pager 에 `📕 가이드 전체 보기` 밖에 없었습니다. 유닛·교재와 같은 형태로 이전/다음 편을 넣었고,
제목이 모두 `TOEIC ` 로 시작해서 pager 에서는 접두어를 떼어 짧게 보여 줍니다(`← 접속사·전치사·접속부사 구분법`).

### 14-5. 홈 「이어서 학습」 ✅

1,000단어를 유닛 순서대로 외우는 사이트인데, 다시 왔을 때 “어디서 이어서 하지”를 알 길이 없었습니다.

- 마지막으로 본 카드를 `toeic1000_lastword` 에 기록하고(`showFlashCard`), 히어로의 `▶ 이어서 학습 n/1,000` 버튼이 **아직 안 외운 단어 중 그 단어부터** 시작합니다.
- 학습 기록이 없는 첫 방문에는 버튼을 숨깁니다(할 일이 겹침). 모두 외운 상태라면 오늘의 복습으로 안내합니다.
- 순서 계산은 `resumeOrder()` 라는 순수 함수로 분리해 회귀 테스트가 직접 검증합니다(`tools/test-home-ux.mjs` **5건**).

### 14-6. 교재 낱개 과 주소 ✅

책 페이지 하나가 12과 · 77KB 인 한 문서라 **“이 과”를 가리키는 주소가 없었습니다**(`grammar/basic.html#ch-07` 은 앵커일 뿐 검색·공유에 쓰기 어렵습니다).

- 과마다 `grammar/basic-01.html` · `conversation/conversation-basic-01.html` 처럼 **평범한 정적 페이지**를 만듭니다(문법 36 + 회화 36 = **72개**).
- 낱개 과 페이지는 `← 책 이름 전체` · `← 이전 과` · `다음 과 →` 로 이어지고, breadcrumb 은 4단계(홈 › 교재 › 책 › 과)입니다. `canonical` 은 각자 자기 주소를 가집니다.
- 책 페이지는 그대로 두고(연속 읽기 흐름 보존), 책마다 「🔗 과 하나씩 따로 보기」 목록으로 12개 과 주소를 안내합니다. 사이트맵에도 함께 들어갑니다.
- 하위 폴더(`basic/01.html`) 대신 같은 폴더의 평범한 파일로 둔 이유: 링크가 단순해지고, 생성물 검증(`tools/verify-generated.mjs`)이 디렉터리 한 단계만 훑어도 걸립니다.

> 내용이 책 페이지와 같은 본문을 담습니다(정본은 책 페이지). 검색 유입·공유를 위한 선택이고, 두 페이지가 서로를 링크합니다.

### 14-7. 홈 묶음 펼침 상태 기억 ✅

53개 섹션을 4묶음으로 접어 두었지만, **펼쳐 둔 상태가 저장되지 않아** 화면을 옮겼다 돌아오면 기본값(어휘 묶음만 펼침)으로 돌아갔습니다.

- 묶음 토글을 `toeic1000_homegroups` 에 저장하고, 홈에 들어올 때 복원합니다.
- 저장에 없는 묶음은 **HTML 기본값을 따릅니다**(나중에 묶음을 추가해도 그대로 펼침/접힘을 정할 수 있게).
- 인쇄용 임시 펼침은 저장하지 않습니다(인쇄하면 전부 펼쳐지는데, 그 상태가 저장되면 다음 방문부터 다 펼쳐집니다).
- 순수 로직은 `tools/test-home-ux.mjs` **5건**이 검증합니다(복원 · 기본값 유지 · false 반영 · 저장 · 깨진 JSON).

### 14-8. 교재 → 앱 딥링크 ✅

낱개 과 페이지는 “이 과”를 알려 주는 주소인데, 앱에서는 **단계만 지정할 수 있고 과는 다시 골라야** 했습니다.

- 낱개 과 페이지에 `✏️ 앱에서 이 과 문제 풀기` → `../index.html?level=basic&ch=5#grammar-quiz` (문법 교재만 — 회화는 앱에 문제 풀이가 없습니다).
- 앱은 `?level=` + `&ch=` 을 읽어 그 과의 연습 문제만 뽑고, 바로 문제를 시작합니다(기존 `?level=` 동작은 그대로).
- 필터가 걸린 사실을 화면에 드러냅니다 — `📖 기초 영문법 5과만 풀고 있어요` + `✕ 과 선택 해제`. (안 보이면 “왜 문제가 3개뿐이지”로 보입니다.)
- 없는 과 번호(`ch=99`)나 다른 단계의 과는 조용히 전체 과로 풀립니다(바깥에서 온 링크·오타 대비). 단계를 직접 바꾸면 필터도 풀립니다.
- 계산은 `parseDeepLink()`·`resolveChapter()` 두 순수 함수로 분리해 테스트 **8건**이 검증합니다.

### 14-9. 색인·메타 감사 강화 ✅

과 단위 페이지 72개가 늘면서 **주소가 어긋나는 사고**를 자동으로 잡아야 했습니다. 기존 감사는 “canonical 이 우리 사이트인가”까지만 봤습니다.

- `canonical` 이 그 페이지의 **배포 주소와 정확히 같은지** 확인합니다(폴더 index 는 폴더 주소).
- `<title>`·`canonical` 이 **다른 페이지와 겹치는지** 확인합니다(검색엔진이 어느 쪽을 색인할지 혼동).
- 낱개 과 페이지는 구조화 데이터에 `position`(몇 번째 과)과 `isPartOf`(어느 책)가 있어야 통과하고, 책으로 돌아가는 링크(`← 책 전체`)도 검사합니다.
- 실제로 `canonical` 을 책 페이지로 돌리고 `<title>` 을 복사해 두 가지가 모두 잡히는지 확인했습니다(검증 후 복구).

### 14-10. 점검 중 함께 드러난 것

- 문법 교재 허브 JSON-LD 의 `name` 이 `중급 중급 영문법` 이었습니다(사용자에게는 안 보이지만 검색엔진이 읽는 값).
- `tools/check-browser.mjs` 의 “회화 교재 카드 4장” 검사가 **고정 대기**에 기대고 있었습니다 — 확장 홈은 `requestIdleCallback` + `data/extra.js`(167KB) 뒤에 그려져
  브라우저가 바쁘면 0장으로 실패했습니다. 실제로 그려질 때까지 기다리도록 바꿔 재현성을 확보했습니다.
- 새 내비게이션을 **실제 브라우저에서 검증하는 검사 18건**을 `tools/check-browser.mjs` 에 추가했습니다(과 12개 인식 · 앵커 유효 · 바 표시/숨김 · 넘침 0px · 스크립트 오류 0건 · 낱개 과 페이지 4건 · 홈 묶음 유지 등).
- 정적 페이지가 55개 → **127개**(낱개 과 72개 추가)로 늘면서 `docs/*.md` 의 `audit:counts` 수도 함께 갱신했습니다. `npm run audit:site` 가 문서 수치를 실제 값과 대조합니다.

### 14-11. 9차 점검 재현 방법

```bash
npm run check          # 홈 UI 101건 · 감사 3종 · 빌드
npm run check:browser  # 과 이동 · 이어서 학습 · 낱개 과 페이지 · 홈 묶음 유지 · 앱 딥링크 포함

# 과 끝 링크·현재 과 바가 6권 전부에 있는지 (각 12개 + #chBar 1개)
grep -c 'class="chnav"' grammar/*.html conversation/*.html
grep -c 'id="chBar"'    grammar/*.html conversation/*.html

# pager 단계명 중복이 남아 있지 않은지 (0건이어야 함)
grep -o '중급 중급[^<]*\|초급 초급[^<]*\|고급 고급[^<]*' grammar/*.html conversation/*.html | wc -l

# 낱개 과 페이지가 72개인지 (문법 36 + 회화 36)
ls grammar/*-[0-9][0-9].html conversation/*-[0-9][0-9].html | wc -l

# 배포 주소와 어긋난 canonical · 중복 title · 과 페이지 구조화 데이터가 0건인지
npm run audit:site | grep -E 'canonical|<title>|position|isPartOf' || echo '0건'

# 교재 → 앱 딥링크가 그 과만 뽑는지 (5과=3문항·1과 아님)
#   브라우저에서 /index.html?level=basic&ch=5#grammar-quiz 을 열고 확인
```

---

## 15. 10차 점검 (2026-09-16) — 내비게이션 심화 · 배포 노출

> 계기: “네비게이션을 더 강화·업그레이드” 요청 + 저장소 전체 점검.
> 9차(§14)에서 교재·가이드·홈 「이어서 학습」을 정리했으므로, 이번에는 **남아 있던 세 곳**을 고쳤습니다 — ① 단어장 유닛 페이지의
> “가로 이동 수단 없음” ② 정적 페이지의 “맨 위로 없음” ③ 홈 깊은 구간의 “섹션을 옮기려면 목차까지 되돌아가야 함”.
> 점검 중 **배포 사이트에 저장소 전체가 공개되고 있다는 사실**을 실측으로 찾아 함께 처리했습니다.

### 15-1. 요약

| 심각도 | 내용 | 상태 |
| --- | --- | --- |
| P1 | 배포된 사이트에서 `docs/`(내부 문서) · `tools/` · `promo/` · `package.json` 이 그대로 내려받아짐 | ✅ robots 색인 차단 + 배포 목록 분리 |
| P2 | 배포 파이프라인이 없어 운영 파일까지 배포될 수 있었음(`main` 루트 그대로) | ✅ `tools/stage-site.mjs` + Actions Pages 워크플로 |
| P2 | `sw.js` 프리캐시 목록이 손 관리라, 회화 교재 3권과 허브 4개가 빠져 있었음 — 이를 잡는 감사가 없었음 | ✅ 감사 추가 + 목록 보강 |
| P2 | 단어장 유닛 페이지는 이전/다음만 있어 멀리 있는 유닛은 허브를 거쳐야 함 | ✅ 유닛 바로가기 목록 |
| P2 | 홈 깊은 구간에서는 섹션을 옮기려고 목차까지 되돌아가야 함(§13-6 에 남긴 항목) | ✅ 섹션 이동 버튼 |
| P3 | 가이드 9편은 이전/다음 편만 있어 중간 편으로 건너뛰기 어려움 | ✅ 가이드 바로가기 목록 |
| P3 | 정적 페이지 124개에 “맨 위로”가 없어 긴 목록을 위로 되짚어야 함 | ✅ 하단 「↑ 맨 위로」 |
| P3 | 정적 페이지 124개가 방문 통계에서 빠져 있었음(검색 유입은 대부분 이 페이지) | ✅ 공용 분석 스크립트 |
| P3 | 문서상 요구 버전(`engines` >=18 · CI 18.x/20.x)과 실제 도구 요구(Node 22+)가 어긋남 | ✅ 정리 |

### 15-2. 단어장 유닛 바로가기 ✅

유닛 페이지 30개는 `← 이전 / 📚 전체 보기 / 다음 →` 만 있어, 17번을 보다 25번으로 가려면 허브로 돌아갔다 다시 들어와야 했습니다.

- 페이지 끝에 `🔢 다른 유닛으로 바로 가기 (30개)` — `<details>` 라 **자바스크립트 없이** 열리고, 닫힌 상태로도 링크가 문서에 남습니다.
- 교재의 「다른 과」 목록과 **같은 `.toc` 카드 모양**을 써서 페이지마다 새 스타일을 만들지 않았습니다. 현재 유닛은 `aria-current="page"` + `· 지금`.
- 같은 목록을 숙어(`units/idioms.html`)에도 붙였습니다(그 페이지에서도 단어장으로 바로 이동).

### 15-3. 가이드 바로가기 ✅

가이드 9편은 순서대로 읽는 코스라 이전/다음 편(§14-4)이면 충분하다고 봤지만, 이미 세 편을 읽고 돌아온 사용자에게는 부족했습니다.
`📕 다른 가이드로 바로 가기 (9편)` 을 같은 형태로 붙였습니다.

### 15-4. 정적 페이지 「맨 위로」 ✅

홈 앱에는 부동 `↑` 버튼이 있었지만 정적 페이지에는 없었습니다(유닛 페이지는 단어 33개가 이어져 길고, 교재 책 페이지는 77KB입니다).

- `page()` 템플릿의 푸터 뒤에 `↑ 맨 위로`(`#top` → `<body id="top">`)를 넣어 **124개 페이지가 한 번에** 갖게 했습니다.
- 인쇄에서는 숨깁니다(이미 `.pager`·`.chnav` 와 함께 인쇄 제외 목록에 넣었습니다).

### 15-5. 홈 깊은 구간 이동 ✅

§13-6 에 “섹션 메뉴가 스크롤과 함께 사라지므로 깊은 구간에서는 맨 위로를 거쳐야 함”이 남아 있었고, 목차 비고정은 의도된 선택이었습니다(스크롤 부담).
그래서 **고정하는 대신 한 섹션씩 옮기는 수단**을 붙였습니다.

- `▲ 이전 섹션` · `▼ 다음 섹션` — 700px 이상 스크롤하면 `맨 위로` 버튼 위에 나타납니다(모호한 화살표 대신 `aria-label`·`title` 로 뜻을 적었습니다).
- 이동은 기존 `scrollToEl()` 을 그대로 쓰므로 **접힌 묶음을 먼저 펼치고** `prefers-reduced-motion` 도 존중합니다. 현재 위치는 기존 `지금: …` 표시가 자동 갱신됩니다.
- `「/」` 단축키 — 검색창은 「단어 목록」 화면에만 있으므로(`showView` 가 다른 화면에서는 숨깁니다), 다른 화면이면 **목록 화면으로 옮긴 뒤 초점**을 줍니다. 입력 중(검색창·메모 등)에는 가로채지 않습니다.

### 15-6. 배포 노출 (P1) ✅

공개 주소를 직접 조회해 보니 **저장소 루트가 그대로 배포**되고 있었습니다 — `/package.json` · `/tools/build-pages.mjs` · `/promo/publish.py` · `/docs/monetization-plan.md` 가 모두 `200`.
`robots.txt` 는 `Allow: /` 라 내부 문서가 색인될 수도 있었습니다. (`promo/config.json` · `.secrets/` 는 `.gitignore` 덕에 `404` — 유출은 없었습니다.)

- **즉시 완화**: `robots.txt` 에 `Disallow: /tools/` · `/promo/` · `/docs/` · `/package.json`.
- **근본 조치**: `tools/stage-site.mjs` 가 공개 목록(`PUBLIC_FILES` · `PUBLIC_DIRS`)만 `_site` 로 복사하고, ① 공개 목록 누락
  ② sitemap 의 모든 URL 이 `_site` 에 있는지 ③ HTML 이 부르는 로컬 자산이 있는지 ④ 운영 경로가 섞이지 않았는지 를 검증합니다.
- `.github/workflows/deploy.yml` — 감사·테스트(`npm run check:ci`)를 통과한 뒤 `_site` 만 Pages 로 올립니다.
  > ⚠️ 저장소 설정에서 Pages → Source 를 **“GitHub Actions”** 로 바꿔야 동작합니다(그 전까지는 기존 브랜치 배포가 그대로 유지됩니다).

### 15-7. sw.js 프리캐시 감사 ✅

프리캐시 목록은 손으로 관리하는데, 이를 검사하는 도구가 없었습니다. 실제로 **어긋나 있었습니다** — `data/conversation-*.js` 3개(홈이 첫 화면에서 내려받는 파일)와
`conversation/` 허브 4개가 빠져 있어, 그 파일이 캐시되기 전에 오프라인이 되면 회화 섹션이 빈 화면으로 보였습니다.

- `tools/audit-site.mjs` 에 **5-1. sw.js 프리캐시 정합성**을 추가했습니다. `sw.js` 를 `vm` 으로 실제 실행해 목록을 읽고
  ① 없는 파일이 적혀 있는지 ② 홈이 내려받는 파일이 모두 들어 있는지 ③ 사이트맵의 섹션 허브가 들어 있는지 를 봅니다.
- 목록을 보강하고 `CACHE_NAME` 을 `v10` 으로 올렸습니다(그 시점 값 — 현재는 v11). 감사가 **실제로 잡는지** 확인했습니다 — 회화 3개를 일부러 빼니
  `sw.js: index.html 이 내려받는 파일이 프리캐시에 없습니다 — data/conversation-basic.js, …` 로 실패했습니다(검증 후 복구).

### 15-8. 정적 페이지 방문 분석 ✅

분석 스크립트(Umami)는 `index.html` · `privacy.html` · `terms.html` 3개에만 있었습니다. 검색 유입은 대부분 정적 페이지로 들어오는데 그 성과가 측정되지 않았습니다.
`build-pages.mjs` 템플릿에 넣어 **124개 페이지가 한 번에** 갖게 했습니다(공개 범위는 `privacy.html` 안내와 같습니다 — 쿠키 미사용 · 익명 통계).

### 15-9. 점검 중 함께 드러난 것

- **`tools/check-browser.mjs` 가 아예 중단되고 있었습니다.** `evaluate()` 로 넘기는 템플릿 리터럴 안의 `split("\n")` 이 실제 줄바꿈으로 치환돼,
  브라우저가 `SyntaxError` 로 실패했습니다(CI 는 이 도구를 돌리지 않아 통과해 왔습니다). `split("\\n")` 으로 고쳤고, 이번 §15-2~15-5 검증이 그대로 돌아갑니다.
- **`.gitignore` 의 `*.pyc` 패턴이 깨져 있었습니다** — 앞 줄과 붙어 `*.pyc# 생성된 쇼츠 영상 (…)` 한 줄이 되어 주석까지 삼켜졌습니다(`__pycache__/` 덕에 티가 나지 않았습니다).
- `tools/serve.mjs` 가 **커밋되지 않은 채** `package.json` 의 `serve` 스크립트만 이를 가리키고 있었습니다 — 새로 받으면 `npm run serve` 가 실패합니다. 추적 대상에 넣고,
  헤더 주석의 “tools/ 는 서빙 대상 경로가 아님” 도 실측에 맞게 고쳤습니다(로컬 서버·Pages 모두 루트를 서빙합니다).
- `promo/README.md` 오타(`뜽니다` → `뜹니다`) — `audit:text` 는 사이트 콘텐츠(17,008개 문자열)를 보므로 `promo/` 는 사각지대입니다.
- `engines`(>=18) · CI 매트릭스(18.x/20.x) vs 실제 도구 요구(전역 `WebSocket` → Node 22+): `>=22` 와 `22.x`/`24.x` 로 맞췄습니다.

### 15-10. 10차 점검 재현 방법

```bash
npm run check:ci       # 감사 3종 + 테스트 3종 + 빌드 + 생성물 최신성 (커밋 전에는 verify 가 당연히 실패합니다)
npm run check:browser  # 섹션 이동 · 「/」 단축키 포함

# 배포 목록만 담기는지 (운영 경로가 섞이면 실패)
npm run stage && ls _site            # tools·promo·docs·package.json 이 없어야 합니다

# 프리캐시 감사 (홈이 내려받는 파일·섹션 허브 대조)
npm run audit:site | grep 'sw.js 프리캐시'

# 정적 페이지 바로가기 목록이 124개 페이지에 있는지
grep -c 'class="jump"' units/*.html guides/*.html | head
grep -c 'class="totop"' units/unit-01.html grammar/basic.html

# 배포 사이트 노출 점검(배포 후) — 404여야 합니다
for p in package.json tools/stage-site.mjs docs/monetization-plan.md promo/publish.py; do
  echo "$(curl -s -o /dev/null -w '%{http_code}' https://toeic.monster/$p)  /$p"
done
```

---

## 16. 11차 점검 (2026-09-16) — 배포본 무게 · 목차 이동 회귀

> 계기: 남은 제안을 모두 적용하고 최종 점검 후 배포. 10차(§15)에서 배포 파이프라인(`_site`)이 생겼으므로,
> §9 부터 미뤄 둔 “배포 경로가 생기면 하자”였던 **index.html 무게 줄이기**를 그 위에서 실제로 적용했습니다.
> 그 과정에서 점검 도구로만 보이던 **목차 이동 회귀**(절반 확률로 제자리로 돌아옴)를 찾아 고쳤습니다.

### 16-1. 요약

| 심각도 | 내용 | 상태 |
| --- | --- | --- |
| P2 | `index.html` 719KB 를 첫 방문마다 다시 받음(압축 전) — §9 에서 “분할은 배포 경로가 생기면” 으로 남긴 항목 | ✅ 배포본에서 인라인 CSS 분리 + HTML 최소화 |
| P2 | 목차에서 고른 섹션이 **약 절반 확률로 제자리로 되돌아옴**(브라우저 스크롤 복원이 이동을 취소) | ✅ 도착할 때까지 확인 후 재이동 |
| P3 | 배포본(분리·최소화된 결과)을 점검할 수단이 없어 “배포에서만 깨지는” 경우를 못 잡음 | ✅ `npm run check:staged` |
| P3 | 루트 `README.md` 없음 · `.gitattributes` 없음(CRLF 혼선) · `measure-home.mjs` 미연결 | ✅ 정리 |

### 16-2. 배포본 무게 줄이기 ✅

원본은 그대로 두고 **`_site` 에 담을 때만** 다듬습니다. `index.html` 이 유일한 원본이라는 규칙(§9)은 그대로입니다.

- 큰 인라인 `<style>`(2KB 초과)을 `assets/app.css` 로 빼고 `<link>` 로 바꿉니다 — 위치가 같아 적용 순서도 그대로입니다.
- 남은 HTML 에서 주석과 태그 사이 공백을 걷어냅니다(텍스트 안쪽 공백은 보존 — `white-space: pre-wrap` 대비).
- 실측: `index.html` **719.5KB → 624.7KB(−94.8KB, −13.2%)** + `assets/app.css` 79.8KB(재방문 시 캐시), 배포본 전체 3,233KB → 2,888KB(−10.7%).
- 검증: `npm run check:staged` 로 **배포본을 그대로** 브라우저 점검(아래 16-4).

**인라인 JS 348KB(48%)는 아직 그대로입니다.** `index.html` 안의 블록을 읽는 도구가 여섯 개라(`audit-content` 배열 추출 · `audit-text` · `test-home-ux` · `test-tts-voice` · `test-grammar-quiz` · `prerender-home` 의 vm 실행), 옮기려면 그 여섯을 함께 손봐야 합니다 — 다음 후보로 남깁니다.

### 16-3. 목차에서 고른 섹션으로 돌아오던 문제 ✅

재현: 목차에서 섹션을 고르면 `y=46123` 대신 `y=2200`(고르기 직전 위치)으로 끝나는 일이 **약 절반** 있었습니다.

- 원인: 목차를 열 때 뒤로가기용으로 **빌린 history 항목을 `closeToc()` 이 `back()` 으로 돌려주는데**, 브라우저가 그 이동에서 스크롤 위치를 복원합니다.
  그 복원이 진행 중이던 `scrollIntoView({behavior:"smooth"})` 를 취소해 화면이 제자리로 돌아왔습니다.
- 시도 1(실패): `back()` 직전에 `history.scrollRestoration = "manual"`, popstate 에서 `auto` 복원 — 복원이 popstate **뒤에** 적용되어 되살아났습니다.
- 채택: **도착할 때까지 확인해 다시 이동**합니다(`keepAt`). 250ms 간격 최대 4회, 섹션 상단이 200px 안에 들어오면 즉시 중단합니다.
  평소에는 한 번 더 확인하고 끝나며, 복원이 늦게 끼어들어도 결국 목표 위치에 섭니다.
- 검증: 수정 전 3회 중 1~2회 실패 → 수정 후 **원본 6회 + 배포본 1회 연속 통과**.

또한 점검 도구가 고정 대기(2.6초)로 재던 부분을 **멈출 때까지 재는 방식**(`waitForScrollSettle`)으로 바꿔, 44,000px 짜리 부드러운 이동이
기계 부하에 따라 중간에서 측정되던 문제도 함께 없앴습니다.

### 16-4. 배포본을 점검하는 수단 ✅

`npm run check:staged` = `npm run stage` + `node tools/check-browser.mjs --site-root _site`.
`--site-root` 는 서빙 폴더만 바꾸므로 같은 100여 항목이 배포본에 그대로 적용됩니다(리포트 첫 줄에 서빙 폴더를 적습니다).
홈 스타일이 인라인인지 `assets/app.css` 인지도 항목으로 확인합니다 — 분리 경로가 틀리면 여기서 걸립니다.

### 16-5. 부수 정리 ✅

- `.gitattributes` — 텍스트는 `eol=lf` 로 고정(Windows `core.autocrlf=true` 환경에서 CRLF 로 받아 생기는 혼선 방지). 추가 후에도 `git status` 변화 없음을 확인했습니다.
- 루트 `README.md` — 공개 저장소인데 안내가 없었습니다. 실행·명령·구조·배포를 짧게 정리했습니다(배포본에는 들어가지 않습니다).
- `npm run measure` — `tools/measure-home.mjs` 만 npm 스크립트가 없었습니다.

### 16-6. 11차 점검 재현 방법

```bash
npm run check:ci        # 감사 3종 + 테스트 3종 + 빌드 + 생성물 최신성
npm run check:browser   # 원본 점검
npm run check:staged    # 배포본 점검(분리·최소화 결과 그대로)

# 배포본 무게(숫자가 리포트에 찍힙니다)
npm run stage | grep 'HTML 최소화'
ls -l _site/index.html _site/assets/app.css

# 목차 이동은 반복 실행으로 확인합니다(수정 전에는 3회 중 1~2회 실패)
for i in 1 2 3; do npm run check:browser | grep '고른 섹션'; done
```

---

## 17. 12차 점검 (2026-09-16) — 배포본 JS 분리 · 성능 실측 · 배포 후 자동 점검

> 계기: 11차에서 남긴 세 가지(인라인 JS 348KB · 성능 수치 · 실서버 자동 점검)를 모두 처리했습니다.

### 17-1. 요약

| 내용 | 상태 |
| --- | --- |
| `index.html` 의 인라인 JS 348KB 를 배포본에서 `assets/app.js` 로 분리 | ✅ 721.5KB → **269.9KB(−63%)** |
| “가벼워졌다”를 숫자로 남길 수단이 없었음 | ✅ `npm run perf` (`tools/measure-perf.mjs`) |
| 배포 회귀(_site 는 맞는데 실서버에서만 깨지는 것)를 자동으로 잡을 수단이 없었음 | ✅ 배포 워크플로의 `smoke` 작업 |
| 다른 출처 자원(웹폰트 CDN·분석)이 응답하지 않으면 브라우저 점검이 빨간불이 됨 | ✅ 문제로 세지 않음(참고로만) |

### 17-2. 배포본 JS 분리 ✅

`tools/stage-site.mjs` 가 `_site` 에 담을 때 큰 인라인 `<script>`(4KB 초과, `type`·`src` 없는 것)를
`assets/app.js` 로 빼고 `defer` 로 불러오게 합니다. JSON-LD(구조화 데이터)와 743B 짜리 테마 스크립트는
그대로 둡니다.

```
index.html   721.5KB (인라인) → 269.9KB  + assets/app.css 79.8KB · assets/app.js 342.2KB
```

- **왜 저장소에서는 쪼개지 않는가**: `index.html` 안쪽 블록을 읽는 도구가 여섯입니다(`audit-content` 배열 추출 · `audit-text` ·
  `test-home-ux` · `test-tts-voice` · `test-grammar-quiz` · `prerender-home` 의 vm 실행). 원본은 그대로 두고
  배포본에서만 빼면 그 여섯을 건드리지 않고 같은 효과를 봅니다(§16-2 와 같은 방식).
- **`defer`**: 앱 스크립트는 문서 맨 뒤에 있고, 그 앞의 데이터 스크립트 39개도 `defer` 입니다. 같은 순서로 실행되고
  (앱은 `document.readyState` 를 보고 시작합니다) 파서를 막지 않아 첫 화면이 먼저 그려집니다.
- 검증: `npm run check:staged` — 배포본 그대로 100여 항목 통과(자바스크립트 오류 · 실패한 요청 0건).

### 17-3. 성능 실측 ✅ `npm run perf`

헤드리스 Chrome 으로 **첫 방문**(빈 캐시)과 **재방문**(캐시·서비스 워커)을 재고, 요청 수 · 압축 전송량 ·
FCP/LCP/DCL/load · 캐시 적중 · 종류별 합계를 보여줍니다. 로컬 계측에는 gzip 을 걸어 배포 조건과 맞췄습니다.
원본과 배포본을 나란히 재려면 `--compare`(대상이 바뀔 때 캐시를 비웁니다 — 안 비우면 먼저 잰 쪽이 받아 둔
웹폰트가 캐시에 남아 착시가 생깁니다).

**로컬 A/B(같은 문서 · gzip · 여러 번 재어 같은 값이 나온 것만 적었습니다)**

| 지표 | 저장소 원본 | 배포본(_site) |
| --- | --- | --- |
| 문서 | 167.2KB | **49.8KB (−70%)** |
| 문서+스타일 | 180.2KB | 79.6KB (−56%) |
| 재방문 전송량 | 0.9KB | **0.3KB (−69%)** |
| FCP · LCP | 560~672ms | **292~308ms (−50%)** |
| DCL | 697ms | 368ms |
| 요청 수 | 65~66개 | 70개 |
| 첫 방문 전송량 | 962~1,012KB | 962KB (총량은 거의 그대로) |

**첫 방문 총량은 줄지 않습니다** — 같은 내용을 파일로 나눈 것이라 총량은 같고(±5% 흔들림),
오히려 앱·스타일이 별도 요청으로 빠집니다. 이 분리의 값은 **문서가 70% 가벼워져 더 일찍 그려지는 것**(FCP/LCP −50%)과
**재방문에서 바뀬 부분만 다시 받는 것**입니다.

**프로덕션(https://toeic.monster · 실제 CDN)**

| 지표 | 12차 이전 | 12차 이후 |
| --- | --- | --- |
| 첫 방문 전송량 | 1,011.9KB | 961.5~1,012.0KB (총량은 거의 그대로) |
| 문서가 받는 스크립트 | 255.5KB (문서 안에 포함) | **0KB** — app.js 100.6KB 로 분리(캐시 대상) |
| 문서 | 149.9KB | **50.2KB (−67%)** |
| 스크립트 | 255.5KB | 306~356KB (app.js 100.6KB 가 분리되어 따로 셈됩니다) |
| FCP · LCP | 1,156ms | 1,032ms |
| 재방문 전송량 | 1.0KB | 1.0KB (안정 상태 — 문서·앱·스타일 모두 캐시) |

문서가 149.9KB → 50.2KB 로 줄고 앱·스타일이 각각 따로 캐시되므로, 둘 중 하나만 바뀨도 그 파일만 다시 받습니다.
재방문 전송량은 서비스 워커가 캐시를 다 채우기 전에 새로고침하면 수십 KB 로 보일 수 있어(배경 재확인),
도구가 그 잔여 요청을 `재방문 잔여:` 줄로 따로 보여 줍니다.

→ **가장 큰 지렛대는 이제 웹폰트(첫 방문의 57%)** 입니다. Pretendard 가변 서체의 하위집합(woff2) 20여 개를
받느라 574KB 를 씁니다. 다음 후보로 남깁니다(굵기·서브셋 축소, 또는 첫 페인트 뒤 로드).

### 17-4. 배포 후 자동 점검 ✅

`.github/workflows/deploy.yml` 에 `smoke` 작업을 넣었습니다 — 배포가 끝나면 **실제 주소**를
`node tools/check-browser.mjs --live` 로 훑어 홈 · 정적 페이지 12개 · 404 · robots · 사이트맵 124개를 확인합니다.
(배포 직후 CDN 을 고려해 20초 쉬고 시작합니다.) 이제 “_site 는 맞는데 배포에서만 깨지는” 것을 푸시 때 바로 잡습니다.

함께: 다른 출처 자원(웹폰트 CDN·분석 스크립트)이 응답하지 않은 것은 **문제로 세지 않고 참고로만** 남깁니다.
그 가용성은 `audit:external` 이 보고, 바깥 사정으로 CI 가 빨간불이 서는 일을 없앴습니다.

### 17-5. 도구 보강

- `check-browser.mjs` — 섹션 이동 뒤 “지금:” 표시는 IntersectionObserver 가 갱신하므로, 고정 대기 대신
  **이동이 멈춘 뒤** 재도록 바꿨습니다(배포본 점검에서 간간히 흔들렸습니다).
- `ci.yml` — `external-links` 작업이 Node 20.x 를 쓰고 있었습니다(`engines: >=22` 와 어긋남). 22.x 로 맞췄습니다.
- `npm run perf` · `perf:compare` · `check:live` 스크립트를 추가했습니다.

### 17-6. 12차 점검 재현 방법

```bash
npm run check:ci            # 감사 3종 + 테스트 3종 + 빌드 + 생성물 최신성
npm run check:browser       # 원본
npm run check:staged        # 배포본(분리·최소화 결과)
npm run perf:compare        # 원본 vs 배포본 — 전송량 · FCP/LCP · 캐시 적중
npm run perf -- --live      # 배포된 사이트 실측
npm run check:live          # 배포된 사이트를 브라우저로 점검(CI smoke 와 같은 것)
```

---

## 18. 13차 점검 (2026-09-18) — 채점 결과 낭독·정적 페이지 Tab 순서

12차까지는 화면에 **보이는** 것을 재었습니다. 13차는 **키보드와 화면 낭독기로만** 쓰는 경우를
따라갈며, 눈으로는 드러나지 않는 세 가지를 찾아 고쳤습니다(모두 감사가 잡지 못하던 항목입니다).

### 18-1. 요약 · 현재 무게

| # | 문제 | 심각도 | 상태 |
| --- | --- | --- | --- |
| 18-2 | 정답을 골라도 **결과가 화면 낭독기에서 읽히지 않았습니다** — 앱 전체에 라이브 영역이 토스트 1곳뿐이었습니다. | P2 | ✅ |
| 18-3 | 정적 페이지 124개에 **본문 바로가기(건너뛰기 링크)가 없었습니다** — 앱에만 있었습니다. | P2 | ✅ |
| 18-4 | 문서·설정이 **실제 배포 방식과 어긋나 있었습니다**(서비스워커 버전 · 서빙 범위 설명 · robots.txt). | P3 | ✅ |

현재 원본 무게(§3 의 409KB 권고 항목에 대한 **현재 값**):

| 항목 | 값 |
| --- | --- |
| `index.html` 원본 | 708KB raw (725,070B) · 167KB gzip |
| 인라인 `<style>` / `<script>` | 76KB / 304KB |
| `data/*.js` | 38개 · 686KB |
| 배포본 문서(`_site`) | 49.8KB + `app.css` 80KB + `app.js` 348KB (캐시 대상) |

원본을 어떻게 나눌지는 **`docs/app-split-plan.md`** 에 단계·검증·되돌리기까지 적어 두었습니다
(이번 점검에서는 코드를 바꾸지 않았습니다 — 도구 14종이 `index.html` 을 입력으로 쓰기 때문에
"문서 분리"는 도구 입력 형식을 함께 바꾸는 별도 작업입니다).

### 18-2. 채점 결과가 화면 낭독기에서 읽히지 않았음 ✅

| | |
| --- | --- |
| **증상** | 퀴즈·시험·리스닝·모의고사·진단·Part 문제은행은 정답을 고른 순간 `“정답입니다! 🎉 / 오답입니다. 정답: X”` 를 결과 상자에 끼워 넣고 **보기 버튼을 비활성화**합니다. 그런데 그 상자가 라이브 영역이 아니어서 낭독기 사용자는 결과를 듣지 못했고, 비활성화된 버튼은 초점을 잃어 다음 Tab 이 문서 처음부터 시작됐습니다. (`aria-live` 는 앱 전체에 토스트 1곳뿐이었습니다.) |
| **조치** | ① 결과 상자를 만드는 **모든 곳**에 `role="status" aria-live="polite" aria-atomic="true"` 를 넣었습니다 — 템플릿 문자열 15곳 + 정적으로 있는 3곳 + 문제은행 카드 + 프리렌더 결과(재생성) = 감사 기준 **28곳**. ② `restoreFocusToFeedback()` 헬퍼를 두어 **초점을 잃었을 때만** 결과 상자로 초점을 옮깁니다(선택 목록·입력 칸으로 답한 경우는 손대지 않습니다). |
| **검증** | `audit-site.mjs` 에 **3-2d** 를 추가했습니다 — 상자 태그에 `aria-live` 가 빠지면 실패합니다. 계측: “채점 피드백 라이브 영역 28개(퀴즈·시험·연습)”. |

### 18-3. 정적 페이지에 본문 바로가기가 없었음 ✅

| | |
| --- | --- |
| **증상** | 정적 페이지는 `상단바 → 빵부스러기 → 본문` 순서라, 키보드 사용자는 **페이지마다 Tab 을 3~4번** 눌러야 본문에 닿습니다. 앱(`index.html`)에는 처음부터 `본문으로 바로가기` 가 있었지만 정적 페이지 124개에는 없었습니다. `404.html` 에는 `<main>` 랜드마크도 없었습니다. |
| **조치** | 생성기(`build-pages.mjs`)의 `page()` 와 404 템플릿에 건너뛰기 링크(`.skip-link` → `<main id="main">`)를 넣고, 공용 스타일(`assets/site.css`)에 규칙을 추가했습니다. 124개 페이지가 한 번에 갖습니다. |
| **검증** | `audit-site.mjs` 에 **3-2c** 를 추가 — 링크나 `<main id="main">` 이 빠지면 실패합니다. 계측: “정적 페이지 124개에 본문 바로가기 링크”. |

### 18-4. 문서·설정이 실제 배포 방식과 어긋났음 ✅

| 대상 | 잘못되어 있던 내용 | 조치 |
| --- | --- | --- |
| `tools/serve.mjs` 헤더 | “GitHub Pages 도 main 브랜치 루트를 그대로 배포하므로 tools/·promo/·docs/ 도 공개됩니다” — 배포는 `_site/`(공개 목록)만 올리므로 사실이 아닙니다 | 실제 구조(스테이징 폴더만 게시)로 고쳤습니다 |
| `robots.txt` | `/tools/` · `/promo/` · `/docs/` 를 `Disallow` — 배포본에 아예 없는 경로입니다 | 필요 없다는 설명으로 바꾸고 `Allow: /` + Sitemap 만 남겼습니다 |
| `docs/ux-review.md` §3 · §4-3 | `index.html` 약 409KB 가 **현재 값처럼** 읽혔습니다 | “(당시 값)” 표시 + 현재 값은 §18-1, 계획은 `docs/app-split-plan.md` 로 연결 |
| `docs/ux-review.md` §9-3 | 서비스워커 캐시 이름 “(현재 v9)” — 실제는 v10 | “그 시점 v9 · 현재는 v10” 으로 정정 |

### 18-5. 남은 권고

| 심각도 | 내용 | 상태(14차, §19) |
| --- | --- | --- |
| P3 | 원본 `index.html` 708KB 단일 파일 | ✅ 1단계(스타일 분리) 실행 → 628KB. 2단계(앱 스크립트 302KB)는 `docs/app-split-plan.md` |
| P3 | 가이드 9편·`grammar/index.html` 의 본문 분량이 665~1,404자 | ✅ 보강 → 1,455~3,143자(§19-2) |

### 18-6. 13차 점검 재현 방법

```bash
npm run check:ci         # 감사 3종(3-2c·3-2d 포함) + 테스트 3종 + 빌드 + 생성물 최신성
npm run check:browser    # 실제 Chrome — 앱 11화면 · 정적 페이지 · 404 · 정적 페이지 스타일

# 채점 결과가 라이브 영역인가 (28개)
node tools/audit-site.mjs | grep '라이브 영역'

# 정적 페이지 124개에 건너뛰기 링크가 있는가
grep -c 'class="skip-link" href="#main"' units/unit-01.html grammar/basic.html 404.html   # 1 1 1

# 문서·설정이 실제와 맞는가
grep -n 'CACHE_NAME' sw.js                      # v11 이어야 합니다(앱 스타일을 프리캐시에 넣으며 올림)
grep -c 'Disallow' robots.txt                   # 0 (배포본에 없는 경로는 막을 필요 없음)
```

---

## 19. 14차(후속 조치) — 13차 권고 실행 (2026-09-18)

13차에서 "별도 작업"으로 남긴 두 가지를 실행했습니다. 두 건 모두 **감사·테스트가 잡지 못하던 항목**이라
(스타일은 도구 여섯의 입력 형식을 바꾸는 일, 본문 분량은 아무 도구도 재지 않는 값) 함께 검사를 추가했습니다.

### 19-1. 원본 `index.html` 스타일 분리 (`app-split-plan` 1단계) ✅

| 항목 | 이전 | 이후 |
| --- | --- | --- |
| `index.html` 원본 | 708KB raw · 167KB gzip · 9,669줄 | **628KB raw(−11%) · 151KB gzip · 7,362줄** |
| 인라인 `<style>` | 80KB | **0KB** |
| `assets/app.css` | 원본에는 없고 배포본에서만 생성 | **원본 파일 80.3KB(gzip 16.7KB)** |
| 배포본 앱 문서(`_site/index.html`) | 270.7KB | 270.7KB (**변화 없음**) |

- `<style>` 을 **같은 자리에서** `<link rel="stylesheet" href="assets/app.css">` 로 바꿔 적용 순서를 그대로 두었습니다.
- 떼어낸 내용이 원래 배포본에서 나가던 것과 같은지 **바이트로 비교**했습니다(헤더 주석 4줄만 차이).
  그래서 배포본 문서 크기도 전송량도 변하지 않았습니다 — 원본만 가벼워졌습니다.
- 회귀 방지를 함께 넣었습니다:
  - `audit-site.mjs` **3-2b-2** — `index.html` 이 `app.css` 를 부르지 않거나 인라인 `<style>` 이 2KB 를 넘으면 실패.
  - `sw.js` — `CORE_ASSETS` 에 `./assets/app.css` 를 넣고 캐시 이름을 `v10 → v11` 로 올렸습니다.
    파일로 빼는 순간 프리캐시 목록에 없으면 오프라인에서 스타일 없는 화면이 됩니다.
  - `test-home-ux.mjs` — 6-1 의 CSS 규칙 검사가 `index.html` 이 아니라 `assets/app.css` 를 읽도록 변경.
    (그대로 두면 11건이 "코드 블록을 찾지 못했습니다"로 실패합니다 — 실제로 먼저 실패해서 찾았습니다.)
- 남은 것: 인라인 앱 스크립트 302KB → `docs/app-split-plan.md` 2단계.

### 19-2. 얇은 페이지 본문 보강 ✅

| 페이지 | 이전(태그 제거 본문 기준) | 이후 |
| --- | --- | --- |
| 가이드 9편 | 779 ~ 1,404자 | **1,455 ~ 2,324자** |
| `guides/index.html` | 1,155자 | 1,605자 |
| `grammar/index.html` | 665자 | **3,143자** |

- 가이드마다 「예문으로 확인하기」·「자주 하는 실수」·「시험장 점검」 같은 실전 항목을 더하고,
  어형 변화·접속사·LC 숫자 가이드에는 어형 세트·유형별 메모를 늘렸습니다(`data/extra.js`).
- `grammar/index.html` 에는 **단계별 안내**(교재 소개 · 이런 분께 · 학습 목표 · 공부법 · 과별 목차 36개 링크),
  **한눈에 보기**(3단계 36과 · 연습 108문항), **학습 순서**(3 → 5단계), **연습 문제 활용법**,
  **자주 묻는 질문**을 넣었습니다(`build-pages.mjs`). 교재 소개·학습 목표 문장은 이미 데이터에 있었지만
  허브에서는 보이지 않던 값입니다.
- 홈은 가이드 카드에 제목·요약만 쓰므로 첫 화면 무게는 늘지 않았습니다(프리렌더 산출물 그대로).

### 19-3. 14차 재현 방법

```bash
npm run check:ci         # 감사 3종(3-2b-2 포함) + 테스트 3종 + 빌드 + 생성물 최신성
npm run check:staged     # 배포본에서 앱 스타일이 실제로 적용되는지

# 앱 스타일이 파일인가(인라인으로 되돌아갔는가)
node tools/audit-site.mjs | grep '앱 스타일'      # assets/app.css 80.3KB · index.html 인라인 0.0KB
node tools/check-browser.mjs | grep '홈 스타일'   # 인라인 0KB · 분리 assets/app.css

# 얇은 페이지 본문 분량
node -e "for (const f of ['guides/part-5-grammar.html','grammar/index.html']) { const s=require('fs').readFileSync(f,'utf8'); const t=s.replace(/<(script|style)[\s\S]*?<\/\1>/g,' ').replace(/<[^>]+>/g,' ').replace(/\s+/g,' ').trim(); console.log(t.length, f); }"
```

---

## 20. 15차 점검 (2026-09-18) — 앱 스크립트 분리(2단계) · 섹션 이동 표시 안정화

### 20-1. 앱 스크립트를 `assets/app.js` 로 분리 ✅

`index.html` 628KB → **290KB**(1,537줄). 353KB짜리 앱 `<script>` 를 `assets/app.js` 파일로 옮기고
`index.html` 은 `<script defer src="assets/app.js"></script>` 로 부릅니다. 인라인 `<script>` 는
첫 페인트 부트스트랩(테마) + JSON-LD 3개만 남았습니다(가장 큰 것 1.5KB).
계획·위험·검증은 `docs/app-split-plan.md` 2단계에 정리했습니다.

- `index.html` 에서 들여쓰기로 코드 블록을 떼어내던 도구 8종(`prerender-home` · `test-home-ux` ·
  `test-grammar-quiz` · `test-tts-voice` · `audit-content` · `audit-text` · `audit-site` · `build-pages`)이
  `tools/app-source.mjs` 의 `readAppSource()` 로 "`index.html` + `assets/app.js`" 를 같은 방식으로 읽습니다.
- 회귀 방지: `audit-site.mjs` **3-2b-3** — 인라인 `<script>` 이 8KB 를 넘거나 `assets/app.js` 를
  부르지 않으면 실패. `sw.js` 프리캐시에 `./assets/app.js` 추가(캐시 `v11 → v12`).
- 실측(`npm run perf:compare`): 첫 방문 965.1KB → 963.9KB(늘지 않음) · LCP 596ms → 332ms.
- 남은 것: 인라인 데이터 배열을 `data/app-data.js` 로 모으는 일은 위험이 커서 미뤘습니다(3단계 후보).

### 20-2. 섹션 이동 「지금:」 표시가 한 박자 늦던 문제 ✅

- 증상: `npm run check:browser` 의 `섹션 이동: 위치 표시가 새 섹션으로 바뀝니다` 검사가
  **간헐적으로 실패**했습니다(같은 검사를 2회 돌려 1회 실패). 배포 후 `smoke` 작업이 랜덤하게
  빨간불이 될 수 있는 상태였습니다.
- 원인: `지금:` 표시는 `IntersectionObserver` 가 갱신하므로, 「다음/이전 섹션」 버튼이나 섹션 칩처럼
  코드로 옮길 때는 스크롤이 멈춘 뒤에야 반영됐습니다. 점검은 정지 직후 값을 읽어 이전 값을 보았습니다.
- 조치: 칩 강조·`지금:` 갱신을 `setCurrentSection(label)` 한 곳으로 모으고, 코드로 옮기는 경로
  (`move()` · 섹션 칩 클릭)가 **곧바로** 부르게 했습니다. 점검은 고정 대기 대신 표시가 실제로 바뀔
  때까지 기다린 뒤 잽니다.
- 확인: `npm run check:browser` 3회 연속 통과(이전에는 간헐 실패).

### 20-3. 학습 데이터를 `data/app-data.js` 로 분리 ✅

`assets/app.js` 345.9KB → **259.6KB**, `data/app-data.js` **85.5KB**(학습 배열·객체 23개).
스타일·스크립트·데이터가 각각 캐시 가능한 파일이 되어, 한쪽만 바뀔 때 다른 쪽은 그대로 재사용합니다.
`index.html` 은 `data/app-data.js` 를 `assets/app.js` 보다 먼저 부릅니다(전역으로 선언되고 앱이 이름으로 씁니다).

- 함께 고친 **잠재 버그**: `prerender-home.mjs` 의 `extractVar` 가 한 줄짜리 빈 배열(`var all = [];`)을
  여러 줄 배열로 오인해 멀리 있는 다른 `];` 까지 삼키고 있었습니다(예전에는 그 덩어리가 우연히 실행돼 통과).
  이제 첫 줄에서 닫히는 배열은 그 줄만 씁니다.
- 회귀 방지: `audit-site` **3-2b-3** 이 `data/app-data.js` 미참조·로드 순서 역전·데이터가 앱 코드로
  되돌아간 경우를 실패로 잡습니다. `sw.js` 프리캐시에 `data/app-data.js` 추가(캐시 `v12 → v13`).
- 확인: `npm run build` 가 "바뀐 내용 없음"(프리렌더 산출물 바이트 동일) · `npm run perf:compare`
  첫 방문 963.2KB → 961.4KB · 재방문 310B → 287B.

### 20-4. 폰트 프리캐시는 실제로 동작한다(확인) ✅

`sw.js` 주석은 "설치 단계에서 CDN CSS 만 미리 담고, woff2 조각은 처음 쓰일 때 fetch 핸들러가 담는다"고
적혀 있는데, 교차 출처 응답은 `cache.add` 가 거부할 수 있어 **실제로 확인**했습니다.

- 확인 방법: 헤드리스 Chrome 에서 서비스워커 등록 뒤 `caches.open("toeic-monster-v13").keys()`
- 결과: 캐시 항목 112개 · Pretendard **CSS 1개**(설치 단계 프리캐시) · **woff2 39조각**(처음 쓰일 때 캐시)
  · `assets/app.js` 1개(2단계에서 추가).
- 근거: jsdelivr 가 `access-control-allow-origin: *` 를 보내므로 설치 단계의 `cache.add` 는 CORS(200)
  응답을 받습니다. 주석에 이 근거를 보강했습니다(동작 변경 없음).

### 20-5. 15차 재현 방법

```bash
npm run check:ci      # 감사 3종(3-2b-2·3-2b-3 포함) + 테스트 3종 + 빌드 + 생성물 최신성
npm run check:browser # 반복 실행 — 섹션 이동 표시가 매번 갱신되는지
npm run check:staged  # 배포본(_site)에서도 앱 스크립트가 실행되는지
npm run perf:compare  # 원본 vs 배포본 전송량 · FCP/LCP

# 스크립트·데이터가 파일인가(큰 인라인·앱 코드로 되돌아갔는가)
node tools/audit-site.mjs | grep -E '앱 스크립트|앱 데이터'
grep -o '<script defer src="data/app-data.js"></script>' index.html
grep -c 'UNITS = \[' data/app-data.js    # 1
```

## 21. 16차 점검 (2026-09-18) — 프리렌더 1단계 · 폰트 렌더 차단 해제 · 얇은 페이지 보강

이번 회차는 "무게"와 "분량" 두 축을 함께 다뤘습니다. 결론부터: **홈 원본이 239.2KB → 196.1KB**,
**첫 방문 FCP·LCP 약 530ms → 약 460ms**, **가장 얇던 가이드 9편이 1.2~1.8천 자 → 1.8~2.6천 자**가 되었고,
그 바닥을 다시 넘지 못하게 감사에 **5-1(본문 분량)** 를 추가했습니다.

### 21-1. 프리렌더 1단계 — 빈도순 어휘를 별도 페이지로

- 프리렌더 블록 가운데 가장 큰 `freqGrid`(48.6KB)만 `units/frequency.html` 로 옮겼습니다.
  홈에는 상위 20개(4.9KB)와 「전체 목록 보기」 링크만 남습니다. 앱이 뜨면 예전처럼 200개를 모두 그립니다.
- 새 페이지는 단어 200개를 50개씩 네 구간으로 나누고, 구간 이동 칩 · 학습 순서 · FAQ ·
  내부 링크(단어장·숙어·30일 플랜·문법 요약)를 담아 7,483자(본문)로 만들었습니다.
- 판단 근거와 실측표, "나머지 블록은 지금 옮기지 않는 이유"는 `docs/prerender-split-plan.md` 에 정리했습니다.
- 부수 정리: `sw.js` 프리캐시에 `units/frequency.html`(지금까지 빠져 있던 `units/idioms.html` 도 함께) 추가,
  캐시 이름 `v13 → v14`. 생성기 주석에 박혀 있던 페이지 수(124)처럼 **시간이 지나면 틀려지는 숫자는 문장에서 뺐습니다**.

### 21-2. 폰트(Pretendard, 외부 CDN) 렌더 차단 해제

- 본문 서체 CSS 는 첫 방문 전송량의 절반 이상을 차지하는데, `<link rel="stylesheet">` 라
  첫 페인트가 그 다운로드를 기다렸습니다. `rel="preload" as="style"` + `onload` 로 바꾸고,
  자바스크립트를 끈 사용자를 위해 `<noscript>` 폴백을 남겼습니다(`index.html`·생성 페이지·404 3곳).
- 실측(`npm run perf`, 같은 조건 3회): **FCP·LCP 528/548/516ms → 488/444/456ms · load 약 1.5s → 약 1.0s**,
  CLS 변화 없음(0.512~0.516). 이득이 큰 편은 아니지만 비용이 없고 되돌리기 쉬워 적용했습니다.

### 21-3. 남은 문제 — CLS 0.51 (기록만)

- 홈 첫 방문 CLS 가 0.51(재방문 0.28)로 기준(0.1)을 크게 넘습니다. 측정 도구에 shift 출처를 임시로 찍어 보니
  웹폰트가 도착해 글꼴이 바뀌는 순간(871ms·1,309ms) `main` 이 위로 밀리는 두 번의 큰 이동이었습니다.
- 지금은 그대로 둡니다: 폰트를 다시 막으면 첫 페인트가 느려지고, `size-adjust`/`ascent-override` 로
  대체 글꼴 메트릭을 맞추는 작업은 효과 검증이 더 필요합니다. **다음 회차 후보**로 남깁니다.

### 21-4. 얇은 페이지 보강 (가이드 9편 + 허브 3개)

| 페이지 | 보강 전(본문) | 보강 후 |
| --- | --- | --- |
| 가이드 9편 | 1,243~1,818자 | **1,754~2,621자** |
| `units/` (단어장 허브) | 1,249자 | **1,801자** |
| `guides/` (가이드 허브) | 1,402자 | **1,757자** |
| `conversation/` (회화 허브) | 1,051자 | **1,663자** |

- 가이드마다 **도입 문단 · 자주 묻는 질문 3문항 · 관련 자료 3개**를 데이터(`data/extra.js`)에 추가하고,
  생성기가 화면과 **같은 문장을 FAQPage 구조화 데이터로**도 넣습니다(둘이 어긋나면 검색엔진이 무시합니다).
- 문답·관련 링크는 허브에서도 공용 헬퍼(`faqSection`·`faqNodes`)로 같은 값을 씁니다.
- 회귀 방지로 감사 **5-1** 을 추가했습니다: 색인 페이지 본문이 800자 미만이면 실패, 1,500자 미만이면 목록 보고.
  현재 가장 얇은 색인 페이지는 `grammar/basic-09.html` 1,922자입니다.
- 비용: 보강한 문답·링크는 `data/extra.js` 에 들어가 홈도 함께 내려받습니다(파일 54.5KB → 58.1KB ·
  첫 방문 총량 +3KB 안쪽, gzip 기준 1KB 남짓). 홈 가이드 카드가 같은 파일을 쓰고 있어 따로 떼어내면
  요청이 하나 늘어나므로, 이 정도는 콘텐츠 이득이 더 크다고 판단했습니다.

### 21-5. 16차 재현 방법

```bash
npm run build          # 생성기 + 프리렌더 (freqGrid 는 상위 20개만 심김)
npm run audit          # 감사 5-1(본문 분량) · 사이트맵 125개 · 문서 수치
npm run perf           # 폰트 preload 전후 FCP·LCP·load·CLS
node -e "const h=require('fs').readFileSync('units/frequency.html','utf8');console.log((h.match(/freq-item|w-word/g)||[]).length)"  # 200
grep -c 'rel="preload" as="style"' index.html units/frequency.html   # 각 1
```
