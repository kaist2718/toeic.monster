# 민감 정보 (API 키·계정 정보) — 절대 커밋 금지
promo/config.json
promo/.secrets/

# 예약 게시·게시 이력·성과·중복 방지 (개인 운영 데이터)
promo/scheduled_posts.json
promo/publish_history.csv
promo/performance.json
promo/posted.json
promo/reports/

# Python 캐시
__pycache__/
*.pyc

# 생성된 쇼츠 영상 (make_shorts.py 산출물 — 재생성 가능)
promo/assets/

# 생성된 인쇄용 워크북 (make_workbook.py 산출물 — 재생성 가능)
workbook/

# 배포용 스테이징 폴더 (tools/stage-site.mjs 산출물 — 재생성 가능)
_site/

# Windows 실행 부산물
promo/*.log

# 폰트 서브셋 원본(6.7MB) — tools/make-font-subset.py 가 필요할 때 받아 두는 캐시
.cache/

# 수익화 전략서 — 별도 비공개 저장소(toeic-monetization-strategy)에서 관리
# 공개 저장소에 실수로 다시 올라가지 않도록 제외합니다.
docs/monetization-strategy.md

