# YouTube Shorts 업로드 (공식 Data API v3)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.0.0

# Instagram Reels 업로드 (비공식 라이브러리)
instagrapi>=2.0.0

# Instagram 2단계 인증(2FA) 자동 입력 (선택) — totp_secret 사용 시
# pyotp>=2.9.0

# TikTok 업로드 (선택 — 불안정, playwright 브라우저 필요)
# 설치: pip install TikTokApi playwright  →  playwright install chromium
# TikTokApi>=6.2.0
# playwright>=1.40.0

# 쇼츠 영상 생성 (make_shorts.py)
Pillow>=10.0.0
imageio-ffmpeg>=0.4.9
# 글리프 존재 확인용 (IPA 네모 방지 폴백)
fonttools>=4.40.0

# 쇼츠 TTS 음성 (make_shorts.py --tts, 인터넷 필요, 무료)
edge-tts>=6.1.0